# FC-030.1E.5 — Real-SQL Startup Ordering, Concurrency, and Failure Conformance

## Status

ACTIVE. E.1 through E.4 are closed/frozen. E.5 is conformance-only and must not introduce new migration, compatibility, provider-selection, or host-lifecycle semantics.

```text
E.5.1  Successful startup states              CLOSED / FROZEN
E.5.2  Migration-stage failure preservation   IMPLEMENTED / AWAITING LOCAL REAL-SQL VERIFICATION
E.5.3  Verification-stage outcomes            NOT STARTED
E.5.4  Concurrent SQL startup                 NOT STARTED
E.5.5  Commit-boundary proof                  NOT STARTED
E.5.6  Non-mutation / failure containment     NOT STARTED
```

Migration 005 and production-reporting SQL persistence remain forbidden until E.6 closes FC-030.1 as a whole.

## Goal

Prove the production SQL startup path against real SQL Server:

```text
startup gate
    → C Exclusive migration/adoption
    → D Shared compatibility verification
    → Compatible only
    → host activation permitted
```

E.5 reuses the frozen `SqlServerPersistenceStartupGate`, `SqlServerMigrationEngine`, and `SqlServerRuntimeSchemaCompatibilityVerifier`.

## Determinism requirements

Every concurrency or timeout proof must use isolated databases, explicit SQL session ids, `sys.dm_tran_locks` observation, bounded completion timeouts, assertions that blocked tasks have not completed, deterministic transaction commit/rollback release, and cleanup that cannot replace the primary failure.

Elapsed time and `Task.Delay` are never authoritative evidence of lock ordering.

## E.5.1 — Successful startup states

Required real-SQL states:

```text
empty database
exact legacy post-004 without ledger
exact current with canonical 001–004 history
exact prefix through 001
exact prefix through 002
exact prefix through 003
```

Every case proves:

```text
gate completes once
activation occurs once
history is exactly 001,002,003,004
live FactoryConnect-owned schema exactly matches Current
final D verification is Compatible
```

The through-002 case retains migration 003 in the startup-level matrix.

Implementation uses one fresh database per theory row. Prefix setup reuses the frozen ledger creator, migration executor, and history store; the assertion path always uses the real startup gate.

Closure evidence:

```text
Release solution build                                       PASS
SqlPersistenceStartupRealSqlSuccessIntegrationTests          6/6 PASS
real SQL states: empty/legacy/current/prefix001/002/003      PASS
git diff --check                                             CLEAN
```

E.5.1 is CLOSED / FROZEN.

## E.5.2 — Migration-stage failures

Required cases:

```text
ledger identity occupied by incompatible object
invalid ledger structure
invalid/malformed history
unledgered partial/incompatible FactoryConnect schema
migration identity/checksum mismatch
migration lock timeout
forced migration 003 failure
```

Each must prove:

```text
FailureKind = MigrationOperationalFailure
CompatibilityResult = null
InnerException is the exact exception object emitted by real C
verification stage is not invoked
activation count = 0
```

The E.5.2 assertion path uses the startup gate's existing internal stage seam only to count D invocation and capture the exact exception object emitted by the real migration stage. The migration stage itself opens a real SQL connection and invokes the frozen `SqlServerMigrationEngine`.

Migration 003 retains the existing retry guarantee: first startup fails with exact prefix 001–002 and correct rollback; after removing the conflict, retry converges to exact current state and activates once.

E.5 does not duplicate C.4's exhaustive internal matrix.

Current implementation: IMPLEMENTED / AWAITING LOCAL REAL-SQL VERIFICATION.

## E.5.3 — Verification-stage outcomes

The existing startup-gate internal stage seam may be used only to coordinate state between the real C engine and real D verifier.

Required cases:

### Database incompatibility

```text
real C succeeds
controlled structural drift is introduced before D
real D returns MigrationSchemaDrift
gate throws DatabaseIncompatible
```

The startup exception must carry the exact immutable D result and ordered diagnostics, have no inner exception, and prevent activation.

### Verification operational failure

```text
real C succeeds
external transaction obtains Exclusive FactoryConnect.SqlMigration applock
real D Shared lock acquisition times out
VerificationOperationalFailure
```

The exact D/provider cause is retained and activation remains zero.

### Verification cancellation

Cancellation after C succeeds but before/during D must propagate as `OperationCanceledException` without a `SqlPersistenceStartupException` wrapper and activation must remain zero.

## E.5.4 — Concurrent SQL startup

Run two independent real startup gates against the same empty database, labelled API and Edge only for diagnostic clarity.

Required sequence:

```text
Gate A enters C with Exclusive applock
Gate B attempts C
SQL Server lock metadata confirms Gate B waits on APPLICATION lock
Gate A completes and releases
Gate B proceeds through idempotent C
both execute D verification
both succeed
```

Prove both release directions where practical.

Final state:

```text
both startup calls succeed
each activation count = 1
one exact migration ledger
001–004 appear exactly once
Current schema exact
final D Compatible
no duplicate migration artifacts
no partial/adoption history
```

## E.5.5 — Commit-boundary proof

Retain one deterministic lock/commit boundary test:

```text
external transaction holds Exclusive migration applock
and an uncommitted FactoryConnect-owned schema mutation
startup gate attempts C and is proven waiting via SQL lock metadata
activation remains zero
external transaction commits
startup proceeds and classifies committed state under frozen C/D precedence
```

Do not force an E-layer classification when C already owns an earlier rejection.

## E.5.6 — Non-mutation and failure containment

For rejection scenarios capture, where practical, the existing `SqlRuntimeCompatibilityPersistentStateSnapshot` vocabulary:

```text
ledger identity and rows
FactoryConnect-owned schema
unrelated sentinel object/value
```

Expected:

```text
C failures preserve C rollback semantics
D operational/incompatible outcomes do not mutate persistent state
activation count remains zero
unrelated objects remain unchanged
```

## Hard exclusions

E.5 must not introduce migration 005, reporting persistence schema, operational-metric SQL storage, changes to migrations 001–004, changes to C transactions, changes to D classifications/diagnostics, new startup failure kinds, API/Edge provider branching, or activation before readiness succeeds.

## Closure gate

Before E.5 closes/freeze, run:

```text
Release solution build
E.5 focused real-SQL matrix
E.1 startup contract tests
E.2 composition tests
E.3 API lifecycle tests
E.4 Edge lifecycle tests
focused C migration/locking regressions
focused D compatibility regressions
full Category=SqlServerIntegration suite
existing API and Edge host regressions
git diff --check
```

After E.5 is green, E.6 performs the whole FC-030.1 closure and frozen-boundary audit. Only E.6 closure permits migration 005 design.
