# FC-030.2C.4 — Manifest / Checkpoint FK Blocker — Freeze / Implementation Authorization

## Status

**CLOSED / FROZEN — MIGRATION 006 CORRECTION PATH AUTHORIZED.**

This document reviews, completes, and freezes the decision recorded by:

```text
docs/features/FC-030.2C.4-manifest-checkpoint-fk-physical-contract-blocker.md
```

The earlier blocker record remains historical evidence authority for:

- the physical-schema contradiction;
- the seven unchanged real-SQL publication failures;
- the disposable-versus-durable release-state investigation;
- the conservative selection of a subsequent migration;
- the narrowly reopened FC-030.2A / FC-030.2B boundary;
- the frozen non-solutions.

This document adds the missing immutable Migration 006 catalog identity, strengthens the preservation/retry/concurrency proof matrix, freezes the architecture decision, and authorizes only the mechanical implementation described below.

No SQL, schema descriptor, checksum, runtime, or C.4 mutation change is introduced by this document.

---

## 1. Frozen physical correction

The physical correction remains exactly:

```text
REMOVE

FK_OperationalMetricProjectionManifest_Checkpoint
  dbo.OperationalMetricProjectionManifest
    (OperationalMetricProjectionProcessorRowId)
  → dbo.OperationalMetricProjectionCheckpoint
    (OperationalMetricProjectionProcessorRowId)

ADD

FK_OperationalMetricProjectionManifest_Processor
  dbo.OperationalMetricProjectionManifest
    (OperationalMetricProjectionProcessorRowId)
  → dbo.OperationalMetricProjectionProcessor
    (OperationalMetricProjectionProcessorRowId)
```

The existing composite manifest-to-projection FK remains unchanged:

```text
FK_OperationalMetricProjectionManifest_Projection

(OperationalMetricProjectionProcessorRowId,
 OperationalMetricProjectionRowId)
→
dbo.OperationalMetricProjection
(OperationalMetricProjectionProcessorRowId,
 OperationalMetricProjectionRowId)
```

No other Migration 005 physical object is reopened.

---

## 2. Migration 006 immutable catalog identity

Migration 006 durable identity is now frozen exactly as:

```text
MigrationId:   6
Name:          CorrectOperationalMetricProjectionManifestParent
Resource:      006_CorrectOperationalMetricProjectionManifestParent.sql
Policy:        EngineOwned
```

The canonical repository resource path SHALL be:

```text
src/FactoryConnect.Persistence.SqlServer/Sql/006_CorrectOperationalMetricProjectionManifestParent.sql
```

These values are architectural authority. They participate in canonical migration catalog identity, migration-history validation, exact-prefix validation, runtime compatibility classification, and checksum verification.

Implementation MUST NOT rename, renumber, alias, or substitute this migration identity without reopening architecture review.

The canonical checksum is intentionally **not** frozen by this documentation-only decision because canonical Migration 006 SQL bytes do not exist yet. The checksum becomes executable frozen evidence only after the authorized SQL resource is created and its canonical bytes are reviewed/proven.

---

## 3. Migration 005 historical immutability

Migration 005 remains historical authority exactly as already frozen:

```text
MigrationId:   5
Name:          OperationalMetricReportingPersistence
Policy:        EngineOwned
Checksum:      53F337BB5B3294D331AB6E363C7301A88D0A4CD090762151CFD1508405DEDFC7
```

The implementation MUST preserve Migration 005:

```text
catalog ID                    unchanged
catalog name                  unchanged
resource identity             unchanged
canonical bytes               unchanged
canonical checksum            unchanged
ledger/history interpretation unchanged
```

Migration 006 corrects the current physical schema prospectively. It does not rewrite historical Migration 005 authority.

---

## 4. Frozen non-solutions

The following remain explicitly forbidden:

```text
insert the initial checkpoint before publication
reorder checkpoint-last choreography
special-case or weaken C.4 mutation semantics
disable/defer the manifest FK during publication
remove or weaken the composite manifest-to-projection FK
change Serializable writer isolation
introduce a global/application publication mutex
change unrelated Migration 005 objects
rewrite Migration 005 canonical bytes/checksum
rewrite the seven publication tests to accommodate the old FK
change C.1, C.2, or C.3 semantics to avoid the schema correction
```

---

## 5. Narrowly reopened / reclosed authority

This freeze closes the narrow architecture reopen as follows:

```text
FC-030.2A
  manifest parent-FK mapping                  CORRECTED / FROZEN
  deterministic corrected FK name            FROZEN

FC-030.2B
  Migration 006 correction path               FROZEN / AUTHORIZED
  post-006 CurrentSchemaDescriptor consequence AUTHORIZED
  migration catalog/checksum proof             AUTHORIZED
  real-SQL schema proof                        REQUIRED

Migration 005
  historical authority                        CLOSED / IMMUTABLE

Migration 006
  identity                                    CLOSED / FROZEN
  implementation                              AUTHORIZED
```

The following remain unchanged:

```text
FC-030.2C.1 publication semantics
FC-030.2C.2 transaction/checkpoint coordinator semantics
FC-030.2C.3 projection acquisition semantics
processor → checkpoint → projection → manifest → evidence lock order
Serializable writer isolation
checkpoint advancement last
C.4 dependency-safe mutation sequence
ReconcileProposed validation-only semantics
```

---

## 6. Authorized Migration 006 implementation boundary

Migration 006 may contain only the schema operations required to replace the manifest parent FK:

```text
ALTER TABLE dbo.OperationalMetricProjectionManifest
  DROP CONSTRAINT FK_OperationalMetricProjectionManifest_Checkpoint

ALTER TABLE dbo.OperationalMetricProjectionManifest
  WITH CHECK ADD CONSTRAINT FK_OperationalMetricProjectionManifest_Processor
  FOREIGN KEY (OperationalMetricProjectionProcessorRowId)
  REFERENCES dbo.OperationalMetricProjectionProcessor
    (OperationalMetricProjectionProcessorRowId)
```

The resulting FK MUST be enabled and trusted.

Equivalent SQL formatting is permitted only to the extent allowed by the frozen repository canonical-SQL rules. No additional DDL is authorized unless mechanically required to express the two operations above under those already-frozen rules.

---

## 7. Frozen implementation / proof matrix

Migration 006 and its post-006 descriptor/catalog changes may close only after all of the following are proven.

### 7.1 Historical identity preservation

```text
1. Migration 005 ID remains 5.
2. Migration 005 name remains OperationalMetricReportingPersistence.
3. Migration 005 resource identity remains unchanged.
4. Migration 005 canonical SQL bytes remain byte-for-byte unchanged.
5. Migration 005 canonical checksum remains
   53F337BB5B3294D331AB6E363C7301A88D0A4CD090762151CFD1508405DEDFC7.
6. Existing canonical migration-history rows 001..005 remain valid without rewrite.
```

### 7.2 Migration 006 exact identity

```text
7. Migration ID = 6.
8. Migration name = CorrectOperationalMetricProjectionManifestParent.
9. Resource = 006_CorrectOperationalMetricProjectionManifestParent.sql.
10. Policy = EngineOwned.
11. Canonical checksum is pinned after SQL bytes are finalized.
12. Catalog ordering is exact canonical 001..006.
```

### 7.3 Exact schema delta

```text
13. Old FK FK_OperationalMetricProjectionManifest_Checkpoint is absent post-006.
14. New FK FK_OperationalMetricProjectionManifest_Processor is present post-006.
15. New FK maps exactly Manifest.OperationalMetricProjectionProcessorRowId
    → ProjectionProcessor.OperationalMetricProjectionProcessorRowId.
16. New FK is created with trusted enforcement (WITH CHECK).
17. New FK is enabled.
18. New FK is trusted in SQL Server metadata.
19. Composite FK_OperationalMetricProjectionManifest_Projection is unchanged.
20. No unrelated column, key, index, CHECK, FK, computed column, collation,
    identity property, default, or table definition changes.
```

### 7.4 Existing data preservation

Starting from a populated exact post-005 database, Migration 006 MUST prove that existing durable data remain unchanged across successful migration.

At minimum compare pre/post values for:

```text
OperationalMetricProjectionProcessor
OperationalMetricProjectionCheckpoint
OperationalMetricProjection
OperationalMetricProjectionManifest
OperationalMetricProjectionEvidence
```

Required result:

```text
processor rows   logically / byte-for-byte unchanged where physically comparable
checkpoint rows  logically / byte-for-byte unchanged where physically comparable
projection rows  logically / byte-for-byte unchanged where physically comparable
manifest rows    logically / byte-for-byte unchanged where physically comparable
evidence rows    logically / byte-for-byte unchanged where physically comparable
```

The migration is structural only; it must not rewrite application data.

### 7.5 Failure / retry atomicity

A forced Migration 006 failure inside the serialized migration transaction MUST prove:

```text
post-failure physical schema = exact post-005
old manifest-checkpoint FK   = present / enabled / trusted
new manifest-processor FK    = absent
application data             = unchanged
migration history            = exact 001..005
```

A subsequent normal retry MUST then:

```text
apply Migration 006 successfully
record canonical migration 006 exactly once
produce exact post-006 CurrentSchemaDescriptor state
preserve existing application data
```

### 7.6 Serialized / concurrent migration execution

Concurrent migration execution against the same exact post-005 database MUST remain governed by the existing FC-030.1 serialized migration authority.

Proof MUST establish:

```text
Migration 006 DDL executes once effectively
Migration 006 history row is recorded exactly once
both callers converge on exact post-006 compatible state
no duplicate ledger row
no partially applied FK state
no bypass of the existing migration lock/transaction boundary
```

### 7.7 Descriptor / compatibility behavior

```text
exact post-005 ledger + schema
  → MigrationPending for migration 006

exact post-006 ledger + schema
  → Compatible

post-006 schema with old FK retained
  → incompatible

post-006 schema with new FK missing/disabled/untrusted/drifted
  → incompatible
```

`CurrentSchemaDescriptor` advances to post-006 authority. Historical post-005 evidence remains historical; it must not be silently rewritten as if Migration 005 had always contained the corrected FK.

### 7.8 Unchanged publication proof

After Migration 006 is applied, rerun the same seven existing:

```text
SqlServerOperationalMetricProjectionPublicationIntegrationTests
```

without changing their C.4 publication sequence to accommodate the FK correction.

Required transitions remain:

```text
empty → populated
retained row replacement
manifest growth
manifest shrinkage
nonempty → empty
component → dependency evidence replacement
ReconcileProposed exact-state validation
```

The seven tests must pass without:

```text
early checkpoint insertion
checkpoint-order change
C.4 mutation workaround
writer isolation change
lock-order change
```

The complete FC-030.2C.3 / FC-030.2C.4 joint publication/concurrency closure matrix remains separately mandatory after this focused physical correction is green.

---

## 8. Architecture decision

The evidence-backed release-state decision is now frozen:

```text
Migration 005 durable deployment state cannot be affirmatively proven disposable-only
→ historical migration 005 must remain immutable
→ correction is delivered by Migration 006
```

The exact Migration 006 identity is frozen by this document.

Implementation is now authorized mechanically within the boundary above.

If implementation discovers that the correction requires any additional schema change, migration-history reinterpretation, C.1–C.3 semantic change, checkpoint choreography change, or C.4 mutation-order change, stop and reopen architecture review.

---

## 9. Final state

```text
FC-030.2C.4    BLOCKED ON PHYSICAL IMPLEMENTATION
               architecture blocker decision CLOSED / FROZEN
               C.4 mutation workaround NOT AUTHORIZED

FC-030.2A      NARROW REOPEN RESOLVED / RECLOSED
               corrected manifest parent FK FROZEN

FC-030.2B      NARROW REOPEN RESOLVED AT CONTRACT LEVEL
               Migration 006 implementation/proof AUTHORIZED

Migration 005  CLOSED / HISTORICAL / IMMUTABLE

Migration 006  IDENTITY CLOSED / FROZEN
               IMPLEMENTATION AUTHORIZED

C.1–C.3        CLOSED semantics remain unchanged
```
