# FC-030.2A — CurrentSchemaDescriptor Post-005 Delta

## Status

**CLOSED / FROZEN.**

This document freezes the exact `CurrentSchemaDescriptor` delta introduced by migration `005_OperationalMetricReportingPersistence.sql`.

`LegacyPost004SchemaDescriptor` remains unchanged.

This document is consumed together with:

- `FC-030.2A-sql-production-reporting-persistence-contract.md`;
- `FC-030.2A-migration-005-owned-object-names.md`;
- `FC-030.2A-physical-authority-sql-evidence-2026-09-06.md`.

No migration is created by this document.

Descriptor semantics are exactly the already-frozen FC-030.1 vocabulary. No new schema-metadata concept is introduced.

---

# 1. Common column metadata rules

Unless explicitly stated otherwise:

```text
schema                         dbo
nvarchar collation             Latin1_General_100_BIN2
identity                       absent
computed                       absent
default                        absent
```

`sys.columns.max_length` expectations:

```text
nvarchar(256)     512
nvarchar(128)     256
nvarchar(64)      128
varbinary(769)    769
varbinary(max)    -1
binary(32)        32
binary(16)        16
uniqueidentifier  16
bigint             8
int                4
smallint           2
tinyint             1
bit                 1
date                3
datetimeoffset(7)  10
decimal(20,0)      13
```

`decimal(20,0)` precision/scale are exactly `20,0`.

Every surrogate row ID explicitly marked identity is:

```text
IDENTITY(1,1)
```

Physical column ordinal is not semantic compatibility authority under FC-030.1.

---

# 2. dbo.OperationalMetricProjectionProcessor

Columns:

```text
OperationalMetricProjectionProcessorRowId bigint          NOT NULL IDENTITY(1,1)
ProcessorKeyBinary                        varbinary(769)   NOT NULL
ProcessorKey                              nvarchar(256)    NOT NULL BIN2
MetricAggregationProcessorRowId           bigint          NOT NULL
MetricInputStreamRowId                    bigint          NOT NULL
```

Owned objects are exactly those frozen in `FC-030.2A-migration-005-owned-object-names.md`.

FK mappings are exact:

```text
FK_OperationalMetricProjectionProcessor_MetricAggregationProcessor
  (MetricAggregationProcessorRowId)
  -> dbo.MetricAggregationProcessor
     (MetricAggregationProcessorRowId)

FK_OperationalMetricProjectionProcessor_MetricAggregationProcessorStream
  (MetricAggregationProcessorRowId, MetricInputStreamRowId)
  -> dbo.MetricAggregationProcessor
     (MetricAggregationProcessorRowId, MetricInputStreamRowId)
  parent candidate key:
     UQ_MetricAggregationProcessor_StreamBinding
```

`ProcessorKeyBinary` is exact `StringOrderKeyV2(ProcessorKey)` by writer construction + reader revalidation; the descriptor validates storage/constraint structure, not the application codec relation.

---

# 3. dbo.OperationalMetricProjectionCheckpoint

Columns:

```text
OperationalMetricProjectionProcessorRowId bigint        NOT NULL
Position                                  decimal(20,0) NOT NULL
```

FK mapping:

```text
FK_OperationalMetricProjectionCheckpoint_ProjectionProcessor
  (OperationalMetricProjectionProcessorRowId)
  -> dbo.OperationalMetricProjectionProcessor
     (OperationalMetricProjectionProcessorRowId)
```

---

# 4. dbo.OperationalMetricProjection

Columns:

```text
OperationalMetricProjectionRowId             bigint            NOT NULL IDENTITY(1,1)
OperationalMetricProjectionProcessorRowId    bigint            NOT NULL
EvaluationKeyCodecVersion                    smallint          NOT NULL
EvaluationKeyHash                            binary(32)        NOT NULL
EvaluationKeyBinary                          varbinary(max)    NOT NULL
MachineId                                    uniqueidentifier NOT NULL
MachineOrderKey                              binary(16)        NOT NULL COMPUTED PERSISTED
PeriodKind                                   tinyint           NOT NULL
PeriodSiteId                                 nvarchar(256)     NOT NULL BIN2
PeriodSiteOrderKey                           varbinary(769)    NOT NULL
ShiftScheduleAssignmentId                    nvarchar(256)     NULL     BIN2
ShiftScheduleAssignmentOrderKey              varbinary(769)    NULL
ShiftId                                      nvarchar(256)     NULL     BIN2
ShiftOrderKey                                varbinary(769)    NULL
ShiftStartsAtUtc                             datetimeoffset(7) NULL
ShiftEndsAtUtc                               datetimeoffset(7) NULL
ProductionBusinessDate                       date              NULL
ProductionOrderPresent                       bit               NOT NULL
ProductionOrderId                            nvarchar(256)     NULL     BIN2
ProductionOrderOrderKey                      varbinary(769)    NULL
OperationPresent                             bit               NOT NULL
OperationId                                  nvarchar(256)     NULL     BIN2
OperationOrderKey                            varbinary(769)    NULL
PartPresent                                  bit               NOT NULL
PartId                                       nvarchar(256)     NULL     BIN2
PartOrderKey                                 varbinary(769)    NULL
OperatorPresent                              bit               NOT NULL
OperatorId                                   nvarchar(256)     NULL     BIN2
OperatorOrderKey                             varbinary(769)    NULL
MetricKey                                    nvarchar(256)     NOT NULL BIN2
MetricKeyOrderKey                            varbinary(769)    NOT NULL
DefinitionVersion                            nvarchar(256)     NOT NULL BIN2
DefinitionVersionOrderKey                    varbinary(769)    NOT NULL
Status                                       tinyint           NOT NULL
MetricValue                                  nvarchar(64)      NULL     BIN2
Unit                                         nvarchar(128)     NOT NULL BIN2
ReasonCode                                   tinyint           NULL
ReasonOperandName                            nvarchar(256)     NULL     BIN2
SourceRevisionPosition                       decimal(20,0)     NOT NULL
```

`MachineOrderKey` computed definition is exactly:

```sql
CONVERT(
    binary(16),
    REPLACE(CONVERT(char(36), MachineId), '-', ''),
    2)
```

and `IsPersisted = true`.

FK mapping:

```text
FK_OperationalMetricProjection_ProjectionProcessor
  (OperationalMetricProjectionProcessorRowId)
  -> dbo.OperationalMetricProjectionProcessor
     (OperationalMetricProjectionProcessorRowId)
```

The two filtered indexes and all PK/UQ/CHECK names/types are exactly those frozen by the owned-object-name appendix.

---

# 5. dbo.OperationalMetricProjectionManifest

Columns:

```text
OperationalMetricProjectionProcessorRowId bigint NOT NULL
OperationalMetricProjectionRowId          bigint NOT NULL
```

FK mappings:

```text
FK_OperationalMetricProjectionManifest_Checkpoint
  (OperationalMetricProjectionProcessorRowId)
  -> dbo.OperationalMetricProjectionCheckpoint
     (OperationalMetricProjectionProcessorRowId)

FK_OperationalMetricProjectionManifest_Projection
  (OperationalMetricProjectionProcessorRowId,
   OperationalMetricProjectionRowId)
  -> dbo.OperationalMetricProjection
     (OperationalMetricProjectionProcessorRowId,
      OperationalMetricProjectionRowId)
  parent candidate key:
     UQ_OperationalMetricProjection_ProcessorRow
```

---

# 6. dbo.OperationalMetricProjectionEvidence

Columns:

```text
OperationalMetricProjectionEvidenceRowId bigint            NOT NULL IDENTITY(1,1)
OperationalMetricProjectionRowId         bigint            NOT NULL
EvidenceKind                             tinyint           NOT NULL
EvidenceOrdinal                          int               NOT NULL
OperandName                              nvarchar(256)     NOT NULL BIN2
OperandNameOrderKey                      varbinary(769)    NOT NULL
ComponentKey                             nvarchar(256)     NULL     BIN2
MetricDimension                          tinyint           NULL
ComponentValue                           nvarchar(64)      NULL     BIN2
ComponentUnit                            nvarchar(128)     NULL     BIN2
InputCount                               decimal(20,0)     NULL
FirstInputTimestamp                      datetimeoffset(7) NULL
LastInputTimestamp                       datetimeoffset(7) NULL
DependencyMetricKey                      nvarchar(256)     NULL     BIN2
DependencyDefinitionVersion              nvarchar(256)     NULL     BIN2
DependencySnapshotCodecVersion           smallint          NULL
DependencySnapshotHash                   binary(32)        NULL
DependencySnapshotBinary                 varbinary(max)    NULL
```

FK mapping:

```text
FK_OperationalMetricProjectionEvidence_Projection
  (OperationalMetricProjectionRowId)
  -> dbo.OperationalMetricProjection
     (OperationalMetricProjectionRowId)
```

---

# 7. dbo.MachineShiftOccurrenceRoster

Columns:

```text
MachineShiftOccurrenceRosterRowId bigint            NOT NULL IDENTITY(1,1)
MachineId                         uniqueidentifier NOT NULL
ProductionDaySiteId               nvarchar(256)     NOT NULL BIN2
ProductionDaySiteOrderKey         varbinary(769)    NOT NULL
ProductionBusinessDate            date              NOT NULL
ProductionLineId                  nvarchar(256)     NOT NULL BIN2
Revision                          decimal(20,0)     NOT NULL
```

There are no FKs from the roster parent.

Logical UQ is exactly:

```text
UQ_MachineShiftOccurrenceRoster_Identity
  (MachineId, ProductionDaySiteOrderKey, ProductionBusinessDate)
```

---

# 8. dbo.MachineShiftOccurrenceRosterOccurrence

Columns:

```text
MachineShiftOccurrenceRosterRowId bigint            NOT NULL
ShiftScheduleAssignmentId         nvarchar(256)     NOT NULL BIN2
ShiftScheduleAssignmentOrderKey   varbinary(769)    NOT NULL
ShiftId                           nvarchar(256)     NOT NULL BIN2
ShiftOrderKey                     varbinary(769)    NOT NULL
ShiftStartsAtUtc                  datetimeoffset(7) NOT NULL
ShiftEndsAtUtc                    datetimeoffset(7) NOT NULL
```

FK mapping:

```text
FK_MachineShiftOccurrenceRosterOccurrence_Roster
  (MachineShiftOccurrenceRosterRowId)
  -> dbo.MachineShiftOccurrenceRoster
     (MachineShiftOccurrenceRosterRowId)
```

PK backing index is exactly NONCLUSTERED and uses:

```text
MachineShiftOccurrenceRosterRowId
ShiftScheduleAssignmentOrderKey
ShiftOrderKey
ShiftStartsAtUtc
ShiftEndsAtUtc
```

---

# 9. PK/UQ/index backing-index authority

Backing-index types are frozen:

```text
PK_OperationalMetricProjectionProcessor                 CLUSTERED
UQ_OperationalMetricProjectionProcessor_ProcessorKeyBinary NONCLUSTERED

PK_OperationalMetricProjectionCheckpoint                CLUSTERED

PK_OperationalMetricProjection                          CLUSTERED
UQ_OperationalMetricProjection_LogicalHash               NONCLUSTERED
UQ_OperationalMetricProjection_ProcessorRow              NONCLUSTERED
IX_OperationalMetricProjection_ShiftWindow               NONCLUSTERED / filtered
IX_OperationalMetricProjection_ProductionDayWindow       NONCLUSTERED / filtered

PK_OperationalMetricProjectionManifest                   CLUSTERED

PK_OperationalMetricProjectionEvidence                   CLUSTERED
UQ_OperationalMetricProjectionEvidence_Operand            NONCLUSTERED
UQ_OperationalMetricProjectionEvidence_Ordinal            NONCLUSTERED

PK_MachineShiftOccurrenceRoster                          CLUSTERED
UQ_MachineShiftOccurrenceRoster_Identity                  NONCLUSTERED

PK_MachineShiftOccurrenceRosterOccurrence                NONCLUSTERED
```

All owned backing indexes / explicit indexes must be enabled.

---

# 10. CHECK/FK operational-state authority

Every owned FK and CHECK defined by `FC-030.2A-migration-005-owned-object-names.md` must be:

```text
disabled = false
trusted / operational state = exact FC-030.1 expected state
```

A disabled/untrusted/drifted definition is incompatible.

No default constraints are introduced by migration 005.

---

# 11. Current / legacy classification

Exact post-005 compatibility state:

```text
canonical migration ledger 001..005
+ exact CurrentSchemaDescriptor including this seven-table delta
=> Compatible
```

Exact post-004 state:

```text
canonical ledger 001..004
+ exact LegacyPost004SchemaDescriptor
=> MigrationPending
```

`LegacyPost004SchemaDescriptor` is never expanded to include 005 objects.

Partial/unledgered/drifted 005 objects are owned artifacts and must not be classified as legacy-adoptable.

---

# 12. Freeze rule

FC-030.2B must implement migration 005 and `CurrentSchemaDescriptor` exactly against this document plus the owned-object-name appendix.

If implementation requires adding/removing a column, changing a type/nullability/collation, changing a computed definition, changing PK/UQ/FK/CHECK/index structure, or expanding the FC-030.1 descriptor vocabulary, stop for architecture review.