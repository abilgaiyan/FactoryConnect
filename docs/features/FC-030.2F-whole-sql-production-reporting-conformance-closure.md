# FC-030.2F — Whole SQL Production Reporting Conformance Closure

## Status

**CLOSED / FROZEN**

FC-030.2F is complete. The SQL production-reporting persistence boundary is proven across migration/startup compatibility, projection publication, public reporting readers, roster authority, restart/recomposition, and whole-integration regression.

No production-code, provider-capability, migration, or schema change is introduced by this closure record.

## Final decomposition

```text
FC-030.2F — Whole SQL Production Reporting Conformance    CLOSED / FROZEN
├── F.1 Authority + coverage inventory                    CLOSED / FROZEN
├── F.2 Write / publication conformance                   PROVEN
├── F.3 Read / reporting / roster conformance             CLOSED / FROZEN
├── F.4 End-to-end SQL production-reporting proof         PROVEN
└── F.5 Closure                                           CLOSED / FROZEN
```

The next roadmap increment is **FC-030.2G — final FC-030.2 closure**.

## Frozen conformance boundary

FC-030.2F does not add or imply a public SQL operational-metric projection-storage capability.

The governing distinction remains:

```text
integration-only projection publication seeding
    ≠
public provider capability
```

The SQL provider continues to expose only the already-proven reporting-side capabilities required by the runtime boundary:

```text
OperationalMetricProjectionQuery
OperationalMetricReportingQuery
MachineShiftOccurrenceRoster
```

It must not advertise:

```text
OperationalMetricProjectionStorage
```

and runtime reporting composition must not require:

```text
IOperationalMetricProjectionStore
```

The internal SQL publication boundary may be used by integration conformance setup to create legitimate persisted projection state. That use does not change provider capability semantics.

## F.1 — authority and coverage inventory

F.1 classified the existing FC-030.2 authority before adding whole-boundary tests.

### Already proven and reused

```text
migration 005/006 physical schema
migration ledger + current-schema verification
startup migration/verification boundary
processor/checkpoint locking
projection/manifest/evidence atomic publication
publication rollback
concurrent publication/replay semantics
summary/detail/evidence reconstruction
stable checkpoint reads
unknown/missing-checkpoint/corruption semantics
reporting-provider parity
SQL roster persistence
roster provider-neutral parity
SQL capability isolation/composition
```

### Cross-boundary proofs assigned to F

```text
publication → readers coherence                    F.2
reporting readers + roster coherence               F.3
migration/startup → publication → restart → report F.4
```

### Explicitly prohibited

```text
SQL OperationalMetricProjectionStorage capability
```

F.1 therefore authorized conformance fixtures only. It did not authorize a new SQL storage contract or production capability.

## F.2 — write / publication conformance

F.2 proves that the already-frozen SQL publication boundary produces reader-visible state that is coherent with a real committed upstream source revision.

Primary fixture:

```text
tests/FactoryConnect.Integration.Tests/
SqlServerOperationalMetricProjectionPublicationReaderConformanceIntegrationTests.cs
```

Relevant implementation/evidence commits:

```text
e0d834b627797dc2de4e730a80f52cffccaec87d
    test: prove SQL publication reader conformance

d7745f9738312b22ddfe01f265c1c512bbc17d50
    test: ground F.2 advancing publication in committed source revision
```

The proven bridge is:

```text
real durable metric-input append
→ committed aggregation checkpoint
→ frozen SQL projection publication boundary
→ committed processor/checkpoint/manifest/projection/evidence
→ frozen SQL summary/detail readers
→ exact source-revision/membership/value/evidence reconstruction
```

The advancing-publication proof additionally establishes:

```text
old committed upstream revision + old complete projection set
→ real next durable input + committed next aggregation checkpoint
→ frozen atomic projection replacement
→ readers expose only new membership/revision/evidence
```

F.2 does not duplicate lower-level transaction/concurrency mechanics already proven by FC-030.2C. It proves the cross-boundary outcome after those mechanics commit.

User-run F.2 gate evidence recorded during the slice:

```text
Release build        PASS
focused F.2          2 / 2 PASS
git diff --check     CLEAN
```

F.2 status: **PROVEN**.

## F.3 — read / reporting / roster conformance

F.3 proves that the public production-day shift reporting path remains roster-authoritative while preserving exact committed metric revision semantics.

Primary fixture:

```text
tests/FactoryConnect.Integration.Tests/
SqlServerProductionDayShiftOperationalMetricConformanceIntegrationTests.cs
```

The required public composition is frozen as:

```text
SqlServerOperationalMetricProjectionQueryReader
→ OperationalMetricReportReader
→ ProductionDayShiftOperationalMetricReader(
      SqlServerMachineShiftOccurrenceRosterStore,
      OperationalMetricReportReader)
→ ProductionDayShiftOperationalMetricQueryReader
```

F.3 intentionally does not inject `IOperationalMetricReportingQueryProvider` into `ProductionDayShiftOperationalMetricReader` and does not bypass the public production-day paging boundary.

Relevant commits:

```text
c8a19a702f38feda9d0c7f2f8e27f8ae28bfb6cc
    test: prove SQL reporting roster conformance

7a6b5ae5005a4c2ddbaced42517d8531f15380e2
    test: satisfy F.3 analyzer gate

29ad32f95dd1d8d673bdee041a3cd946e8bf3c0c
    test: split F.3 roster conformance cases

7d58282813f5be4eebc34cc53cd698b38a624560
    test: satisfy F.3 naming analyzer
```

The final two acceptance cases prove:

```text
rostered shift
→ surfaces through public production-day paging
→ exact machine / production day / line / processor identity
→ exact published metric semantics
→ non-null SourceRevision
→ SourceRevision == committed aggregation checkpoint

same-machine + same-day off-roster shift
→ projection legitimately persisted
→ omitted from roster
→ does not surface in ReportingPage.Items
```

Final user-run F.3 evidence:

```text
Release build        PASS
focused fixture      2 / 2 PASS
git diff --check     CLEAN
```

F.3 status: **CLOSED / FROZEN**.

## F.4 — end-to-end SQL production-reporting proof

F.4 proves the complete frozen vertical boundary against a newly created isolated SQL database and a real restart/recomposition boundary.

Primary fixture:

```text
tests/FactoryConnect.Integration.Tests/
SqlServerProductionReportingEndToEndIntegrationTests.cs
```

Implementation commit:

```text
70f249f34c21a7bb9dda2c429e5cd02a078e52e5
    test: prove F.4 SQL reporting restart path
```

The fixture executes:

```text
new isolated FactoryConnect-uninitialized SQL database
    ↓
first SQL persistence composition
    ↓
IPersistenceStartupGate.EnsureReadyAsync()
    ↓
migrate to current + runtime compatibility verification
    ↓
real durable metric-input append
    ↓
real committed aggregation checkpoint
    ↓
integration-only SQL projection publication seeding
    ↓
public DI-resolved IMachineShiftOccurrenceRosterStore commit
    ↓
assert IOperationalMetricProjectionStore absent
    ↓
dispose first ServiceProvider
    ↓
build a completely new SQL persistence/reporting ServiceProvider
    ↓
IPersistenceStartupGate.EnsureReadyAsync() again
    ↓
assert IOperationalMetricProjectionStore still absent
    ↓
resolve public IProductionDayShiftOperationalMetricQueryReader
    ↓
read persisted production-day shift report
    ↓
prove exact roster + metric + committed source revision survived recomposition
```

This is the required end-to-end persistence proof:

```text
migration/startup compatibility
→ legitimate persisted upstream revision
→ projection publication
→ public roster persistence
→ process-composition boundary
→ startup compatibility verification again
→ public reporting reconstruction
```

It establishes that persisted reporting truth survives service-provider disposal and reconstruction without relying on in-memory state from the first composition.

Final user-run F.4 evidence on 2026-09-09:

```text
dotnet build FactoryConnect.sln -c Release
Build succeeded in 107.1s

focused F.4 real-SQL proof
FactoryConnect.Integration.Tests test net10.0 succeeded (16.7s)
Test summary: total: 1, failed: 0, succeeded: 1, skipped: 0, duration: 16.6s

full integration regression
FactoryConnect.Integration.Tests test net10.0 succeeded (1650.8s)
Test summary: total: 850, failed: 0, succeeded: 850, skipped: 0, duration: 1650.4s

git diff --check
CLEAN
```

F.4 status: **PROVEN**.

## F.5 — closure review

The closure review finds no remaining FC-030.2F conformance gap.

### Required boundaries proven

| Boundary | Evidence | Result |
|---|---|---|
| Existing SQL migration/schema authority remains valid | Reused FC-030.1 / FC-030.2A-B gates | PROVEN |
| Projection publication reconstructs coherently through SQL readers | F.2 | PROVEN |
| Advancing committed revision replaces visible projection membership/evidence coherently | F.2 | PROVEN |
| Public production-day reporting is roster-authoritative | F.3 | PROVEN |
| Off-roster persisted projections do not leak into public production-day reporting | F.3 | PROVEN |
| Public report exposes exact committed source revision | F.3 | PROVEN |
| Empty database migrates/verifies through production startup gate | F.4 | PROVEN |
| Projection + roster state survives first composition disposal | F.4 | PROVEN |
| Recomposition runs startup verification again | F.4 | PROVEN |
| Public reporting reconstructs persisted state after recomposition | F.4 | PROVEN |
| SQL does not advertise/resolve `OperationalMetricProjectionStorage` | F.4 plus frozen D/E composition | PRESERVED |
| Whole integration regression after F.4 | 850 / 850 | PASS |
| Release build | user-run gate | PASS |
| Repository whitespace gate | `git diff --check` | CLEAN |

### No frozen-contract drift found

F.2-F.4 did not require reopening:

```text
migration 005/006 physical authority
CurrentSchemaDescriptor semantics
projection publication transaction semantics
reader checkpoint semantics
roster replacement/revision semantics
public reporting-reader semantics
SQL provider capability declaration
host reporting composition
```

No contradiction was discovered requiring a production repair or architecture amendment.

### No prohibited capability introduced

The final vertical fixture explicitly proves `IOperationalMetricProjectionStore` is not available from either the first or recomposed SQL service provider.

Therefore the conformance work does not accidentally transform integration publication seeding into a runtime persistence capability.

## Final FC-030.2F conclusion

FC-030.2F has fulfilled its sole purpose: prove that the independently frozen SQL persistence, publication, roster, and reporting authorities compose into one coherent production-reporting persistence boundary.

The final supported statement is:

> A FactoryConnect SQL Server database can be migrated and verified to the current repository schema, accept legitimate persisted operational-metric projection and roster state through the already-frozen boundaries, survive reporting-service recomposition, and reconstruct the exact roster-authoritative public production-day report with the committed source revision, without exposing an unsupported operational-metric projection-storage capability.

No additional FC-030.2F implementation is authorized after this closure unless a contradiction is discovered during FC-030.2G or merge/main regression.

## Next step

```text
FC-030.2G — Final FC-030.2 Closure                 NEXT
```

FC-030.2G should reconcile the completed FC-030.2A-F evidence into the final SQL production-reporting persistence closure, then handle branch/merge readiness as a separate repository-integration concern.
