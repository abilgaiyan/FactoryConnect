# FC-030.2A — Physical-Authority Real-SQL Evidence — 2026-09-06

## Status

**PASS / PHYSICAL FEASIBILITY PROVEN.**

This evidence records the disposable `FactoryConnect.Fc0302APhysicalAuthorityCheck` run against the post-`10289cad` FC-030.2A candidate design.

Target:

```text
SQL Server ProductVersion  17.0.1000.7
ProductLevel               RTM
Edition                    Express Edition (64-bit)
.NET target                net10.0
```

The checker created only local temporary tables. It did not create migration 005 or modify repository schema state.

---

# 1. B4 processor V2 authority

Observed PASS:

```text
x vs x\0 distinct UQ slot                         PASS
769-byte ProcessorKeyBinary UQ insert             PASS
exact duplicate rejected                          PASS
exact lookup distinguishes x / x\0                PASS
UPDLOCK,HOLDLOCK distinguishes x / x\0            PASS
```

This proves the corrected processor authority is physically viable:

```text
ProcessorKeyBinary
  varbinary(769) NOT NULL
  exact StringOrderKeyV2(ProcessorKey)
  sole SQL lookup / UQ / CAS-lock identity authority
```

The rejected raw-big-endian-UTF-16 V1 representation is not used.

---

# 2. B3 / V2 maximum-width key creation

Observed PASS:

```text
Evidence operand UQ ~777 bytes                    PASS
Roster parent UQ ~788 bytes                       PASS
Occurrence NONCLUSTERED PK ~1566 bytes            PASS
Occurrence exact duplicate rejected               PASS
```

Therefore the candidate exact occurrence identity remains viable as:

```text
PRIMARY KEY NONCLUSTERED
(
  MachineShiftOccurrenceRosterRowId,
  ShiftScheduleAssignmentOrderKey,
  ShiftOrderKey,
  ShiftStartsAtUtc,
  ShiftEndsAtUtc
)
```

No identity shortening or hash equality authority is required.

---

# 3. B1 nullable CHECK totality

Every exercised invalid shape was rejected by SQL:

```text
present=0 with nonnull value                       PASS
present=0 with nonnull key                         PASS
present=1 null value                               PASS
present=1 empty value                              PASS
present=1 null key                                 PASS
calculated null value                              PASS
calculated with reason                             PASS
unavailable with value                             PASS
unavailable no reason                              PASS
reason code outside domain                         PASS
empty reason operand when present                  PASS
```

Representative valid row:

```text
PASS
```

This confirms the explicit `IS NULL` / `IS NOT NULL` + `DATALENGTH` predicates do not rely on SQL CHECK `UNKNOWN` acceptance for the exercised shape families.

---

# 4. MachineOrderKey

Observed PASS:

```text
PERSISTED expression + filtered index creation     PASS
known/boundary/10,000 random Guid V1 parity        PASS
```

The exact candidate expression is physically accepted and indexable:

```sql
CONVERT(
    binary(16),
    REPLACE(CONVERT(char(36), MachineId), '-', ''),
    2)
PERSISTED
```

The produced bytes matched the frozen Guid V1 authority for the known/boundary/random corpus.

---

# 5. SHA-256 characterization

Observed PASS:

```text
SQL HASHBYTES('SHA2_256', ...)
== .NET SHA256.HashData(...)
```

for representative inputs including a 1 MiB `varbinary(max)` value.

This remains target characterization evidence only. Canonical hash authority remains the writer/reader SHA-256 contract over exact FCEK/FCDS bytes.

---

# 6. Backing-index metadata

Observed PASS:

```text
ProcessorKeyBinary UQ is NONCLUSTERED              PASS
Occurrence complete-identity PK is NONCLUSTERED    PASS
Occurrence index is_primary_key = 1                PASS
```

This validates the candidate backing-index type required by the physical contract.

---

# 7. Final harness result

```text
FC-030.2A PHYSICAL-AUTHORITY CANDIDATE: PASS
```

Consequences:

```text
B1 nullable CHECK totality        PHYSICAL PROOF PASS
B3 roster key/index type          PHYSICAL PROOF PASS
B4 processor V2 authority         PHYSICAL PROOF PASS
MachineOrderKey                   PHYSICAL PROOF PASS
SHA2_256 characterization         PHYSICAL PROOF PASS
key backing-index type            PHYSICAL PROOF PASS
```

The remaining FC-030.2A work is documentation authority only:

```text
freeze owned object names
freeze exact post-005 CurrentSchemaDescriptor delta
synchronize closure documents
explicitly close/freeze FC-030.2A physical authority
```

Migration `005_OperationalMetricReportingPersistence.sql` MUST remain absent until that documentation freeze is complete.