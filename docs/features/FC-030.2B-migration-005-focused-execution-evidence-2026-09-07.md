# FC-030.2B — Migration 005 + CurrentSchemaDescriptor Focused Execution Evidence

Date: 2026-09-07

Status: **FOCUSED GATES PASS / FC-030.2B REMAINS ACTIVE**

This document records the focused execution evidence for FC-030.2B after the migration-005 schema, post-005 `CurrentSchemaDescriptor`, legacy post-004 adoption path, migration catalog authority, and single-descriptor catalog-rendering corrections were completed.

## Repository state under test

The focused gates were executed after the post-005 descriptor was restored as the single executable authority:

```csharp
public static SqlSchemaDescriptor Current { get; } =
    SqlRepositoryPost005SchemaDescriptor.Create(LegacyPost004);
```

The temporary `SqlRepositoryPost005CatalogAuthority` transformation layer had been deleted before this evidence was collected.

The following boundaries remained unchanged during the final catalog-rendering correction:

- migration `005_OperationalMetricReportingPersistence.sql`
- `LegacyPost004`
- `SqlFragmentCanonicalizer`
- `SqlSchemaComparator`
- SQL metadata-reader behavior

Migration 005 canonical checksum remains frozen at:

```text
53F337BB5B3294D331AB6E363C7301A88D0A4CD090762151CFD1508405DEDFC7
```

## Gate 1 — Migration 005 exact schema authority

Command:

```powershell
dotnet test tests\FactoryConnect.Integration.Tests\FactoryConnect.Integration.Tests.csproj `
  --filter "FullyQualifiedName~SqlServerMigration005SchemaAuthorityIntegrationTests"
```

Result:

```text
Test summary: total: 4, failed: 0, succeeded: 4, skipped: 0, duration: 55.0s
Build succeeded in 83.5s
```

This proves the focused migration-005 schema authority matrix, including:

- `LegacyPost004` remains the 13-table pre-005 authority;
- `Current` is the 20-table post-005 authority;
- migration 005 has the expected embedded catalog identity;
- a fresh SQL database migrated through 005 exactly matches `SqlRepositorySchemaDescriptors.Current`;
- `MachineOrderKey` is observed from real SQL metadata as `binary(16)`, computed, persisted, and non-null under the frozen computed-column contract.

## Gate 2 — Actual migration engine

Command:

```powershell
dotnet test tests\FactoryConnect.Integration.Tests\FactoryConnect.Integration.Tests.csproj `
  --filter "FullyQualifiedName~SqlServerMigrationEngineIntegrationTests"
```

Result:

```text
FactoryConnect.Integration.Tests test net10.0 succeeded (161.4s)

Test summary: total: 18, failed: 0, succeeded: 18, skipped: 0, duration: 161.1s
Build succeeded in 175.6s
```

This proves the actual migration engine can complete the current catalog through migration 005, including the FC-030.2B legacy-adoption transition:

```text
unledgered exact post-004
→ validate frozen migration 001..004 catalog prefix
→ create migration ledger
→ record 001..004
→ execute and record 005
→ validate exact Current post-005 schema
→ commit atomically
```

It also proves exact ledger prefixes through migration 005 can execute only the pending suffix and finish at the current repository state.

## Gate 3 — Migration catalog authority

Command:

```powershell
dotnet test tests\FactoryConnect.Integration.Tests\FactoryConnect.Integration.Tests.csproj `
  --filter "FullyQualifiedName~SqlMigrationCatalogTests"
```

Result:

```text
FactoryConnect.Integration.Tests test net10.0 succeeded (6.3s)

Test summary: total: 64, failed: 0, succeeded: 64, skipped: 0, duration: 6.3s
Build succeeded in 21.1s
```

This proves:

- deterministic catalog identity now covers migrations 001..005;
- historical checksums 001..004 remain frozen;
- migration 005 checksum is frozen literally;
- canonical bytes and descriptor checksums agree;
- duplicate ID, duplicate name, and duplicate resource validation are exercised independently;
- migration 003 remains the only grandfathered embedded-transaction migration.

## Focused conclusion

The previously blocking implementation questions are closed by execution evidence:

```text
Migration 005 exact physical schema         PASS
Post-005 Current descriptor exact match     PASS
MachineOrderKey computed metadata           PASS
Legacy post-004 adoption through 005        PASS
Actual migration-engine final validation    PASS
Migration catalog 001..005 authority        PASS
Single post-005 descriptor authority        PASS
```

## Remaining closure gates

FC-030.2B remains **ACTIVE** until the broader regression evidence is recorded:

1. runtime schema-compatibility regression;
2. complete integration suite;
3. repository whitespace gate (`git diff --check`);
4. final FC-030.2B evidence summary and closure review.

No FC-030.2B closure or freeze is asserted by this document alone.
