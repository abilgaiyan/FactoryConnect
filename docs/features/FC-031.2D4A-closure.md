# FC-031.2D.4A — Joint Provider Result / Payload Conformance Closure

Status: CLOSED / FROZEN

## 1. Closure basis

FC-031.2D.4A is closed and frozen against the bounded implementation completed on:

```text
feature/fc-031-2d-state-activity-continuity
```

Final reviewed implementation head before this closure record:

```text
57eeac74ef9f3586dd335de2da7ca88edb8396b5
```

Focused verification evidence supplied for that head:

```text
FactoryConnect.Integration.Tests
Filter      FullyQualifiedName~InMemoryMachineStateActivityAuthorityStoreConformanceTests
Result      12 / 12 PASS
Failed      0
Skipped     0
Duration    8.2s

Release build
Result      PASS

Repository hygiene
git diff --check             CLEAN
git diff --check main...HEAD CLEAN

Final implementation review
Blocking findings            NONE
```

This closure does not authorize any later FC-031 slice.

## 2. Frozen 4A authority

FC-031.2D.4A now freezes the portable joint state/activity authority surface around the coherent tuple:

```text
P = MachineStateActivityProjection
S = DurableMachineStateChangedEvent[]
A = DurableMachineActivityPeriod[]
E = EvaluationAuthority

Joint observable publication = (P, S, A, E)
```

The implementation authority includes:

```text
4A.1  coherent P + E snapshot contract
4A.2  defensive joint publication proposal contract
4A.3  proposal-local intrinsic validation
4A.4  canonical structural complete-payload comparison
4A.5  joint-specific publication result algebra
4A.6  uncomposed in-memory joint provider
4A.7  focused provider conformance proof
```

The reference provider remains deliberately uncomposed.

## 3. Frozen classification order

The authoritative publication classification remains:

```text
1. intrinsic proposal-local validation
2. complete-payload ExactReplay comparison
3. dual CAS validation
4. current-state advancement validation
5. revision-exhaustion classification
6. atomic NewPublication
```

Critical precedence remains:

```text
ExactReplay before dual-CAS rejection
ExactReplay before revision exhaustion
```

Intrinsic validation is proposal-local only and MUST NOT depend on current provider state or expected CAS tokens.

Expected tokens remain excluded from replay identity:

```text
ExpectedProjectionPosition
ExpectedAuthorityRevision
```

Therefore an identical complete payload resubmitted with stale expected tokens remains eligible for:

```text
Accepted / ExactReplay
```

while a non-replay proposal with stale expected tokens remains:

```text
Conflict
```

## 4. Frozen intrinsic-validation boundary

Intrinsic proposal validation establishes only proposal-local coherence, including:

```text
Projection.ProcessorId == EvaluationIdentity.StateProcessorId
Projection.StreamId    == EvaluationIdentity.ObservationStreamId
Projection.Position    == EvaluationIdentity.EvaluatedThrough
Projection.State       == EvaluationIdentity.MachineState

output processor/stream == target projection processor/stream
output position         <= target projection position

Projection.Signals are deterministically ordered
Projection.Signals have unique keys using ordinal-ignore-case identity
StateChanges are strictly increasing by durable position
ActivityPeriods are strictly increasing by durable position
no duplicate ordered output identities
```

The lower advancement bound is not intrinsic.

For a non-replay proposal whose CAS tokens identify the current coherent state, advancement-relative validation compares against the provider-owned current projection:

```text
target position > current projection position
all newly proposed output positions > current projection position
all newly proposed output positions <= target projection position
```

Contradictory/non-forward advancement returns Conflict without mutation or revision allocation.

## 5. Frozen canonical replay identity

Exact replay compares the complete canonical joint payload structurally.

Replay comparison includes:

```text
Projection
    ProcessorId
    StreamId
    Position
    Signals — ordered structural equality
    State
    ActiveState
    ActiveStartedAt

StateChanges
    same count
    same deterministic order
    same element values

ActivityPeriods
    same count
    same deterministic order
    same element values

EvaluationAuthorityReplayIdentity
    StateProcessorId
    ObservationStreamId
    EvaluatedThrough
    MachineState
    LastConsumedInstanceId
    AppliedContinuityPolicy
```

Replay comparison excludes:

```text
ExpectedProjectionPosition
ExpectedAuthorityRevision
provider-assigned StateProjectionAuthorityRevision
```

The final focused conformance repair proves structural negatives across all joint dimensions, including changed projection signals, active context, state-change payload, activity-period payload, last-consumed instance, and applied continuity policy.

Deterministic ordering and duplicate rejection are proved for projection signals, state changes, and activity periods.

## 6. Frozen result algebra

The authority-level meanings remain inherited:

```text
NewPublication
ExactReplay
Conflict
RevisionExhausted
```

FC-031.2D.4A uses joint-specific result records so conflict/exhaustion/acceptance can expose the coherent joint snapshot rather than only EvaluationAuthority.

The existing reusable semantic types remain:

```text
EvaluationAuthorityReplayIdentity
EvaluationAuthorityPublicationDisposition
```

## 7. Frozen provider behavior

The uncomposed in-memory reference provider freezes these behaviors:

```text
fresh publication
    → revision 0

changed strictly-forward publication
    → current revision + 1

identical complete payload + stale expected tokens
    → ExactReplay
    → same revision
    → no mutation

non-replay + stale expected token(s)
    → Conflict
    → no mutation

matching CAS + non-forward target
    → Conflict
    → no mutation

matching CAS + output outside current→target advancement
    → Conflict
    → no mutation

changed forward publication at ulong.MaxValue
    → RevisionExhausted
    → no mutation

exact replay at ulong.MaxValue
    → Accepted / ExactReplay
    → no revision allocation
```

The provider publishes projection, derived outputs, and EvaluationAuthority as one provider-owned atomic visibility unit under its in-memory authority state.

Processor and observation-stream lineages remain isolated.

## 8. Final conformance authority

The focused FC-031.2D.4A conformance suite proves:

```text
fresh and forward publication
stale-token identical-payload ExactReplay
stale-token different-payload Conflict
matching-CAS non-forward Conflict
matching-CAS output-range Conflict
intrinsic cross-component rejection
projection signal ordering/uniqueness rejection
state-change ordering/duplicate rejection
activity-period ordering/duplicate rejection
complete structural replay negatives across P/S/A/E
revision exhaustion without mutation
ExactReplay at ulong.MaxValue
complete quiescent P/S/A/E visibility
processor/stream provider-level isolation
```

Final focused evidence:

```text
12 / 12 PASS
```

No blocking findings remain.

## 9. Frozen scope boundary

This closure authorizes no work beyond FC-031.2D.4A.

The following remain explicitly unauthorized:

```text
FC-031.2D.4B or later proof slices
failure-injection hooks
lost-ack wrapper execution
concurrent-publisher harnesses
MachineStateActivityProcessor integration
Preserve/Reset processor execution
processor-local retry policy
DI registration or composition
FC-031.2D.5
FC-031.2E+
SQL current-state persistence
Core current-state semantic reader
HTTP API
dashboard work
```

The reference provider remains uncomposed until a later separately authorized boundary.

## 10. Authoritative transition

The checkpoint is now:

```text
FC-031.2D.4A  Joint Provider Result / Payload Conformance  CLOSED / FROZEN
FC-031.2D.4B+                                            NOT AUTHORIZED
FC-031.2D.5                                             NOT AUTHORIZED
FC-031.2E+                                              NOT AUTHORIZED
```

FC-031.2D.4A is inherited authority for all later state/activity authority work and MUST NOT be reopened unless concrete repository evidence demonstrates a contradiction.
