# FC-031.2D.4 — Closure Record

Status: CLOSED / FROZEN

## 1. Authoritative inventory

FC-031.2D.4 is closed and frozen against the reviewed documentation-only proof inventory recorded at:

```text
6160ce58b867794e1ec6ba49e1aee2da4da831c9

docs/features/FC-031.2D4-failure-replay-competition-proof-inventory.md
```

Documentation review result:

```text
Findings      NONE
Review        GREEN
Scope         CLEAN
```

This closure record supersedes the inventory document's transitional `DOCUMENTATION RECORD / REVIEW REQUIRED` status. The inventory content itself remains the frozen semantic authority for FC-031.2D.4.

## 2. Frozen proof authority

FC-031.2D.4 freezes the future proof obligations for the joint state/activity authority around the complete observable tuple:

```text
P = MachineStateActivityProjection
S = DurableMachineStateChangedEvent[]
A = DurableMachineActivityPeriod[]
E = EvaluationAuthority

Joint observable state = (P, S, A, E)
```

The reviewed proof matrix freezes all of the following dimensions:

```text
complete (P,S,A,E) observation
intrinsic-invalid rejection before replay/CAS/exhaustion classification
pre-commit failure and cancellation atomicity
successful fresh and strictly-forward publication
retained-proposal provider ExactReplay
fresh-invocation NoAdvance recovery
partial-prefix skipping from coherent current state
dual projection-position / authority-revision CAS
complete structural replay equality and negative cases
same-position and backward conflicts
different-proposal and identical-proposal competition outcomes
revision exhaustion and exact replay at ulong.MaxValue
quiescent atomic visibility
processor/runtime failure propagation
Preserve recovery at provider and fresh-processor layers
Reset recovery at provider and fresh-processor layers
stale mapped-runtime cursor handling
processor/stream lineage isolation
prohibition on reconstructing a lost prior proposal after invocation/restart
```

The 35-case logical matrix in the authoritative inventory is frozen as the minimum semantic proof surface. It does not require 35 physical test methods; later authorized implementation may use theory/data-driven proof while preserving every independently observable obligation.

## 3. Frozen recovery-layer distinction

The following distinction is authoritative and must not be collapsed by later implementation:

```text
provider proposal retry
    retained identical complete proposal
    → Accepted / ExactReplay

fresh processor calculation
    coherent P/E reread
    + no genuinely new incorporated input
    → NoAdvance
    → no joint proposal
    → no PublishAsync call

runtime recovery
    coherent reread governs
    already-incorporated prefix is skipped
    only genuinely new suffix may be evaluated
```

A process or invocation restart does not imply durable recovery of the previous proposal.

Later implementation MUST NOT require reconstruction of the previous proposal from current `P/E` plus candidate observations merely to obtain provider `ExactReplay`.

Processor-local retry of an ambiguous publication using a still-retained proposal remains an optional implementation strategy. FC-031.2D.4 does not freeze or authorize such a retry policy.

## 4. Frozen classification and atomicity authority

The frozen 2D.3 publication ordering remains inherited unchanged:

```text
1. intrinsic joint-payload validation
2. complete canonical replay comparison
3. dual-CAS comparison
4. strictly-forward changed-publication validation
5. revision-exhaustion classification
6. atomic publication of P/S/A/E
```

Critical precedence remains:

```text
ExactReplay before CAS rejection
ExactReplay before revision exhaustion
```

At required quiescent observation cuts, a failed or competing publication may expose only:

```text
the complete old tuple
or
the complete accepted new tuple
```

Mixed projection/output/authority state is forbidden.

## 5. Frozen mapped-runtime boundary

`MappedObservationProcessingRuntime` has no independent generic processing checkpoint for the state/activity path.

The durable mapped-input cursor remains:

```text
MachineStateActivityProjection.Position
```

A later runtime cycle may have selected overlapping candidate observations from a stale cursor, but the processor's coherent `P + E` read is authoritative for deciding what remains unincorporated.

Fully incorporated input converges through `NoAdvance`; a partially incorporated batch skips the incorporated prefix and evaluates only the genuinely new suffix.

## 6. Frozen scope boundary

This closure authorizes no implementation.

The following remain explicitly outside FC-031.2D.4 closure authority:

```text
production-code mutation
provider implementation
test implementation
failure-injection hooks
concurrency harnesses
processor-local retry-policy implementation
DI registration or composition changes
FC-031.2D.5 implementation or proof
FC-031.2E or later work
SQL current-state persistence
Core semantic current-state reader
HTTP API
dashboard work
```

The existing repository test patterns identified by the inventory remain reusable examples only. They do not themselves satisfy the frozen joint `(P,S,A,E)` proof obligations.

## 7. Authoritative transition

The FC-031.2D checkpoint is now:

```text
FC-031.2D.1  Evaluation Authority Store Contract          CLOSED / FROZEN
FC-031.2D.2  Preserve / Reset Continuity Semantics        CLOSED / FROZEN
FC-031.2D.3  Atomic Authority Integration Contract        CLOSED / FROZEN
FC-031.2D.4  Failure / Replay / Competition Proof         CLOSED / FROZEN
FC-031.2D.5  In-Memory Composition / Conformance          NOT AUTHORIZED
FC-031.2E+                                               NOT AUTHORIZED
```

FC-031.2D.4 is therefore inherited authority for all later work and MUST NOT be reopened unless concrete repository evidence demonstrates a contradiction.

No implementation decomposition, 2D.5 work, or later slice is authorized by this closure record.