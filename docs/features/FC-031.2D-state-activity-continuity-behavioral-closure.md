# FC-031.2D — State / Activity Continuity Behavioral Slice Closure

Status: **CLOSED / FROZEN**

This is a closure-only record for the FC-031.2D behavioral inventory and implementation. It introduces no production, test, provider, runtime, persistence, Edge, or dependency-injection behavior.

## Closure evidence

```text
Reviewed implementation     14ae1792dbba6b3098ef868b38353ca51bb51333
Focused proof               12 / 12 PASS
Release build               PASS
Both diff checks            CLEAN
Blocking findings           NONE
Scope compliance            PASS
```

The validated branch also includes the compile-only conformance-proof correction at `5f312596f7a16110ffab41172efd5d31220370b1`. That correction changes no production semantics and does not alter the reviewed implementation authority recorded above.

## Frozen behavioral authority

The behavioral slice now freezes the following portable semantics:

- deterministic state/activity continuity calculation from a coherent projection plus evaluation-authority snapshot;
- fresh bootstrap with no prior projection or evaluation authority;
- coherent restoration of established projection and evaluation authority state;
- same-instance continuation from restored signal, state, and active-activity context;
- `Preserve` behavior across instance boundaries, including retention of prior evaluator context;
- `Preserve` cross-instance activity completion when a new-instance observation causes the next state transition, with the emitted output attributed to that new observation instance and sequence;
- `Reset` behavior across instance boundaries, including clearing signals, restoring `Unknown`, abandoning prior active context without a synthetic completion, and evaluating the new observation from that reset baseline;
- independent application of continuity policy at multiple forward instance boundaries;
- `LastConsumedInstanceId` derived from the instance at the final incorporated durable observation;
- stable continuity-policy reference attribution for each logical evaluation publication;
- full overlap classified as `NoAdvance`, with no publication attempt;
- partial overlap skipping the authoritative prefix and evaluating only the forward suffix, with the first forward observation compared against the restored `LastConsumedInstanceId`;
- deterministic joint publication material covering expected projection position, expected evaluation-authority revision, target projection, ordered state-change delta, ordered activity-period delta, and evaluation replay identity;
- semantic batch-partition convergence for fixed initial authority, policy, and ordered observations, including partitions exactly at `Preserve` and `Reset` instance boundaries;
- cumulative ordered state-change and activity-period history convergence across equivalent batch partitions;
- authority revision equality and per-publication delta grouping explicitly excluded from batch-partition semantic equivalence;
- optional uncomposed processor result mapping where `NewPublication` and `ExactReplay` succeed, `Conflict` and `RevisionExhausted` fail, and `NoAdvance` succeeds without calling `PublishAsync`;
- no automatic reread, recalculation, or retry after provider conflict or revision exhaustion.

## Recovery boundary

This slice proves calculation-level fresh recovery behavior:

```text
current authority through N + complete overlap
    -> NoAdvance

current authority through N + overlapping prefix + forward suffix
    -> skip authoritative prefix
    -> compare first forward instance to restored LastConsumedInstanceId
    -> evaluate forward suffix only
```

Provider-retained exact-proposal replay remains the provider-level `ExactReplay` authority already frozen in FC-031.2D.4C.

Production runtime recovery is not closed by this behavioral slice. Runtime cursor cutover, production lost-ack recovery, and composed mutation ownership remain FC-031.2D.5 responsibilities.

## Explicitly deferred / excluded

The behavioral closure does **not** authorize or record any of the following as implemented:

- replacement of the existing `MachineStateActivityProcessor`;
- changes to `MappedObservationProcessingRuntime`;
- production runtime cursor cutover;
- Edge registration or dependency-injection composition changes;
- production joint-authority provider composition;
- production end-to-end lost-ack recovery closure;
- SQL persistence;
- FC-031.2D.5 implementation;
- FC-031.2E or any later FC-031 surface.

## Final classification

```text
FC-031.2D behavioral slice   CLOSED / FROZEN
FC-031.2D.5                  NEXT / AWAITS SEPARATE AUTHORIZATION
FC-031.2E+                   NOT AUTHORIZED
```

No frozen FC-031.2D decision is reopened by this record.
