# FC-031.2D.4B — Pre-Commit Failure and Cancellation Atomicity Closure

Status: CLOSED / FROZEN

## 1. Closure basis

FC-031.2D.4B is closed and frozen against the bounded implementation completed on:

```text
feature/fc-031-2d-state-activity-continuity
```

Final reviewed implementation head before this closure record:

```text
1456d75d5047361770533592e0cc8fba29ce6e08
```

Focused verification evidence supplied for that head:

```text
FactoryConnect.Integration.Tests
Scope       focused FC-031.2D.4A + FC-031.2D.4B conformance
Result      21 / 21 PASS
Failed      0
Skipped     0
Duration    7.6s

Release build
Result      PASS

Repository hygiene
git diff --check             CLEAN
git diff --check main...HEAD CLEAN

Final bounded implementation review
Blocking findings            NONE
```

This closure does not authorize any later FC-031 slice.

## 2. Frozen 4B authority

FC-031.2D.4B freezes the pre-commit failure and cancellation atomicity proof for the uncomposed in-memory joint state/activity authority provider established by FC-031.2D.4A.

The governing joint tuple remains:

```text
P = MachineStateActivityProjection
S = state-change publication/history
A = activity-period publication/history
E = EvaluationAuthority
```

The reference provider remains deliberately uncomposed.

The implementation authority closed by this record is limited to:

```text
provider-internal NewPublication-path fault mechanism
deterministic F1–F5 pre-commit proof checkpoints
pre-lock cancellation proof for fresh and established lineages
complete P/E and state/activity history no-mutation oracle
no output leakage
no authority-revision consumption
proof that ExactReplay, Conflict, and RevisionExhausted bypass fault injection
focused 4A + 4B provider conformance proof
```

No portable abstraction was extended with fault-injection semantics.

## 3. Frozen cancellation boundary

The provider performs one authoritative cancellation observation before acquiring its mutation lock:

```csharp
cancellationToken.ThrowIfCancellationRequested();

lock (_gate)
{
    ...
}
```

The frozen observable contract is:

```text
cancellation observed by the provider's pre-lock check
    → OperationCanceledException
    → no mutation
```

This applies to both fresh and established lineages.

Once the pre-lock check succeeds:

```text
publication is non-interruptible
later cancellation need not be observed
cancellation while waiting for _gate need not abort
cancellation during publication need not abort
normal classification and commit may complete
```

FC-031.2D.4B adds no cancellation check inside the lock, during materialization, or immediately before commit.

This boundary is CLOSED / FROZEN.

## 4. Frozen classification and fault-seam placement

The FC-031.2D.4A classification order remains unchanged:

```text
1. intrinsic proposal-local validation
2. complete-payload ExactReplay
3. dual CAS
4. current-state advancement validation
5. revision exhaustion
6. atomic NewPublication
```

FC-031.2D.4B fault injection executes only after a proposal has passed the replay, CAS, advancement, and revision-exhaustion gates and has entered the valid changed `NewPublication` materialization path.

Therefore:

```text
ExactReplay
    → bypass fault seam

Conflict
    → bypass fault seam

RevisionExhausted
    → bypass fault seam

valid changed forward NewPublication
    → F1–F5 may execute
```

The fault mechanism remains provider-internal and uncomposed.

No 4B behavior changes the result precedence frozen by 4A.

## 5. Frozen F1–F5 pre-commit checkpoints

The in-memory reference provider freezes these deterministic proof checkpoints:

```text
F1  before new-publication materialization

F2  after EvaluationAuthority construction

F3  after publication-output copies
    LastStateChanges
    LastActivityPeriods

F4  after cumulative-history construction
    StateChangeHistory
    ActivityPeriodHistory

F5  immediately before
    _entries[key] = next
```

These checkpoints describe actual reference-provider materialization work.

They are not portable storage-provider API requirements and do not belong to `IMachineStateActivityAuthorityStore` or any other provider-neutral abstraction.

## 6. Frozen atomic visibility boundary

The single authoritative commit point remains:

```csharp
_entries[key] = next;
```

All deterministic F1–F5 proof points occur before this assignment.

The visibility invariant is:

```text
before _entries[key] = next
    → old tuple only

after _entries[key] = next
    → complete new tuple only
```

No partial `(P,S,A,E)` state is authorized or observable.

Any exception raised at F1–F5 propagates before the dictionary assignment and leaves provider authority unchanged.

## 7. Frozen no-mutation oracle

For an established lineage, every injected F1–F5 failure is required to preserve the complete observable provider state exactly.

The frozen oracle is:

```text
ReadAsync()              == pre-failure snapshot
ReadStateChanges()       == pre-failure state-change history
ReadActivityPeriods()    == pre-failure activity-period history
ProjectionRevision       == pre-failure revision
```

The snapshot equality includes the authoritative projection and evaluation authority semantics:

```text
Projection
    ProcessorId
    StreamId
    Position
    Signals
    State
    ActiveState
    ActiveStartedAt

EvaluationAuthority
    StateProcessorId
    ObservationStreamId
    EvaluatedThrough
    MachineState
    LastConsumedInstanceId
    AppliedContinuityPolicy
    ProjectionRevision
```

No state-change or activity-period output from a failed proposal may leak into cumulative provider history.

## 8. Frozen fresh-lineage atomicity

For a fresh lineage, authoritative pre-lock cancellation or an authorized pre-commit fault leaves the lineage entirely absent:

```text
ReadAsync()              == null
ReadStateChanges()       == empty
ReadActivityPeriods()    == empty
```

No projection, derived output history, evaluation authority, or externally observable revision becomes visible.

A later successful first publication therefore remains revision `0`.

## 9. Frozen revision non-consumption

Revision allocation remains part of the same atomic NewPublication materialization and has no independently committed side effect.

For established authority at revision `R`:

```text
failed F1–F5 publication
    → current revision remains R

subsequent valid changed publication
    → next committed revision remains R + 1
```

For a fresh lineage:

```text
failed first publication
    → no authority exists
    → no revision consumed

subsequent successful first publication
    → revision 0
```

No hidden sequence or revision counter advances independently of the joint dictionary assignment.

## 10. Focused conformance authority

The focused FC-031.2D.4A + FC-031.2D.4B conformance proof now establishes the inherited 4A provider algebra together with the 4B atomicity boundary.

The 4B-specific proof covers:

```text
pre-cancelled fresh publication
    → OperationCanceledException
    → lineage absent

pre-cancelled established publication
    → OperationCanceledException
    → exact old P/E snapshot
    → exact old state/activity histories
    → revision unchanged

F1–F5 established-lineage failures
    → exception propagates
    → exact old P/E snapshot
    → exact old histories
    → no output leakage
    → no revision consumption

fresh-lineage pre-commit failure
    → complete lineage remains absent
    → later successful first publication remains revision 0

ExactReplay
Conflict
RevisionExhausted
    → provider fault mechanism is not invoked
```

Final focused evidence:

```text
21 / 21 PASS
```

Release build passed, both repository diff checks are clean, and the final bounded review reports no blocking findings.

## 11. Provider-composition boundary

The in-memory joint authority provider remains intentionally uncomposed.

FC-031.2D.4B does not authorize:

```text
DI registration
production ownership wiring
MachineStateActivityProcessor integration
continuity-policy execution
provider retry orchestration
```

The provider-internal fault seam exists only to prove reference-provider atomicity and is not part of any public or portable composition contract.

## 12. Frozen scope boundary

This closure authorizes no work beyond FC-031.2D.4B.

The following remain explicitly unauthorized:

```text
lost acknowledgement / post-commit ambiguity
ExactReplay retry orchestration
competing publishers
processor integration
Preserve / Reset execution
LastConsumedInstanceId continuity integration
continuity-policy application in the processor
DI composition
FC-031.2D.4C+
FC-031.2D.5
FC-031.2E+
SQL current-state persistence
Core current-state semantic reader
read-only current-state API
dashboard work
```

No 4B closure statement should be interpreted as authority to enter those areas.

## 13. Authoritative transition

The checkpoint is now:

```text
FC-031.2D.4B contract
    CLOSED / FROZEN

FC-031.2D.4B implementation
    CLOSED / FROZEN

Focused 4A+4B proof
    21 / 21 PASS

Release build
    PASS

Repository hygiene
    git diff --check             CLEAN
    git diff --check main...HEAD CLEAN

Blocking findings
    NONE

FC-031.2D.4C+
FC-031.2D.5
FC-031.2E+
    NOT AUTHORIZED
```

FC-031.2D.4B is inherited authority for later state/activity continuity work and MUST NOT be reopened unless concrete repository evidence demonstrates a contradiction.
