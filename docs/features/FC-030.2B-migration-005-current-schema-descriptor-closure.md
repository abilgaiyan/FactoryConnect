# FC-030.2B — Migration 005 + CurrentSchemaDescriptor — Closure / Freeze

Status: **CLOSED / FROZEN**

Closure date: 2026-09-07

Final evidence:

```text
85106cb3c620b73890848fd63b1faf159710474c
docs: record FC-030.2B final migration 005 evidence
```

## Frozen outcome

FC-030.2B establishes the executable SQL Server migration-005 and post-005 repository schema authority required by FC-030.2A.

The frozen repository relationship is:

```text
LegacyPost004
    historical 13-table descriptor
    exact unledgered legacy-adoption authority

Migration 005
    physical DDL authority for the seven production-reporting tables

Current
    post-005 20-table executable repository descriptor

OwnedObjects
    current 20-table FactoryConnect-owned recognition authority
```

The executable current descriptor is exactly:

```csharp
public static SqlSchemaDescriptor Current { get; } =
    SqlRepositoryPost005SchemaDescriptor.Create(LegacyPost004);
```

There is no secondary descriptor transformation/rewrite authority.

## Frozen migration 005 identity

```text
MigrationId:   5
Name:          OperationalMetricReportingPersistence
Policy:        EngineOwned
Checksum:      53F337BB5B3294D331AB6E363C7301A88D0A4CD090762151CFD1508405DEDFC7
```

Migration 005 owns exactly these seven tables:

1. `OperationalMetricProjectionProcessor`
2. `OperationalMetricProjectionCheckpoint`
3. `OperationalMetricProjection`
4. `OperationalMetricProjectionManifest`
5. `OperationalMetricProjectionEvidence`
6. `MachineShiftOccurrenceRoster`
7. `MachineShiftOccurrenceRosterOccurrence`

Their columns, SQL types, nullability, collation, identity/computed properties, PK/UQ/FK/CHECK/index structures, filtered-index predicates, and owned object names are frozen by FC-030.2A and implemented by this slice.

## Catalog-rendered definition rule

Migration 005 remains the physical DDL authority.

For executable schema comparison, `SqlRepositoryPost005SchemaDescriptor` directly stores the expected SQL Server catalog-rendered forms for migration-005 computed, CHECK, and filtered-index definitions.

This is intentionally not implemented through:

- broader SQL-fragment canonicalization;
- metadata rewriting;
- comparator semantic expansion;
- a second post-005 transformation authority.

FC-030.1B-R remains frozen:

- SQL Server computed metadata is read verbatim;
- comparison is lexical after the frozen canonicalizer;
- `MachineOrderKey` remains computed and persisted exactly as physically created by migration 005.

## Frozen legacy adoption transition

An exact unledgered post-004 database is handled as:

```text
exact physical migrations 001..004
→ validate frozen 001..004 catalog prefix
→ create migration ledger
→ record 001..004
→ execute and record pending catalog suffix (005)
→ validate exact Current post-005 schema
→ commit
```

This occurs inside the existing serialized migration transaction/lock boundary.

No unledgered post-005 schema is adoptable as legacy post-004.

## Runtime compatibility boundary

Runtime compatibility verification remains read-only.

The migration-005 expansion updates supported-state fixtures without changing runtime classification semantics:

```text
MigrationPending
    exact ledger prefix through 004

DatabaseNewerThanSupported
    first unsupported migration ID >= 6

LegacyAdoptionRequired
    exact unledgered post-004 physical schema

Compatible
    exact 001..005 history + exact post-005 Current schema
```

## Final execution gates

```text
SqlServerMigration005SchemaAuthorityIntegrationTests   4/4 PASS
SqlServerMigrationEngineIntegrationTests              18/18 PASS
SqlMigrationCatalogTests                              64/64 PASS
Runtime compatibility regression                      65/65 PASS
Previously affected full-suite regression set         62/62 PASS
FactoryConnect.Integration.Tests                      699/699 PASS
git diff --check                                      CLEAN
```

No skipped tests were reported in the final full integration run.

## Closure decision

No blocking finding remains.

FC-030.2B is therefore **CLOSED / FROZEN**.

Future work must not silently change any of the following FC-030.2B authorities:

- migration-005 ID, name, transaction policy, resource identity, canonical checksum, or DDL;
- the seven migration-005 owned tables or their frozen physical structures;
- `MachineOrderKey` computed/persisted authority;
- the direct `Current = SqlRepositoryPost005SchemaDescriptor.Create(LegacyPost004)` relationship;
- `LegacyPost004` as the exact 13-table historical adoption descriptor;
- current 20-table owned-object recognition;
- catalog-rendered migration-005 CHECK/filter/computed descriptor definitions;
- legacy adoption advancing from exact post-004 through 005;
- read-only runtime compatibility semantics.

If later implementation requires changing any of those semantics rather than consuming them, reopen architecture review instead of modifying this frozen slice implicitly.