# FC-030.2B — Migration 005 + CurrentSchemaDescriptor — Final Evidence

Date: 2026-09-07

Status recorded by this document: **IMPLEMENTATION COMPLETE / CLOSURE REVIEW READY**

## Scope

FC-030.2B implements the frozen FC-030.2A migration-005 physical authority and the post-005 `CurrentSchemaDescriptor`, including the FC-030.1B-R computed-column descriptor amendment.

The executable repository authority is single-source:

```csharp
public static SqlSchemaDescriptor Current { get; } =
    SqlRepositoryPost005SchemaDescriptor.Create(LegacyPost004);
```

`LegacyPost004` remains the frozen historical 13-table descriptor. `Current` is the post-005 20-table descriptor. Repository owned-object recognition now covers the current 20-table schema.

No post-processing/rewrite descriptor authority remains.

## Migration 005 identity

```text
MigrationId:   5
Name:          OperationalMetricReportingPersistence
Policy:        EngineOwned
Checksum:      53F337BB5B3294D331AB6E363C7301A88D0A4CD090762151CFD1508405DEDFC7
```

Migration 005 creates the seven FC-030.2A-owned tables:

1. `OperationalMetricProjectionProcessor`
2. `OperationalMetricProjectionCheckpoint`
3. `OperationalMetricProjection`
4. `OperationalMetricProjectionManifest`
5. `OperationalMetricProjectionEvidence`
6. `MachineShiftOccurrenceRoster`
7. `MachineShiftOccurrenceRosterOccurrence`

The post-005 descriptor stores the expected SQL Server catalog-rendered computed, CHECK, and filtered-index definitions directly. Migration 005 remains the physical DDL authority; the descriptor is the executable expected-catalog authority used for compatibility comparison.

## Focused execution evidence

### Migration-005 exact schema authority

```text
SqlServerMigration005SchemaAuthorityIntegrationTests
Total:     4
Succeeded: 4
Failed:    0
Skipped:   0
Result:    PASS
```

This proves:

- `LegacyPost004` and post-005 `Current` remain distinct authorities;
- migration catalog identity for 005 is exact;
- a fresh migration-005 database exactly matches `Current`;
- the frozen persisted `MachineOrderKey` computed authority matches live SQL Server metadata.

### Migration engine

```text
SqlServerMigrationEngineIntegrationTests
Total:     18
Succeeded: 18
Failed:    0
Skipped:   0
Result:    PASS
```

This includes:

- fresh installation through 005;
- exact ledger prefixes 0 through 5;
- completed-database no-op behavior;
- exact unledgered post-004 adoption;
- adoption recording 001..004 then executing/recording 005 in the same migration boundary;
- final exact-current schema validation.

### Migration catalog

```text
SqlMigrationCatalogTests
Total:     64
Succeeded: 64
Failed:    0
Skipped:   0
Result:    PASS
```

This confirms the 001..005 catalog, duplicate-ID/name/resource validation, frozen 001..004 prefix identities, and the pinned 005 checksum.

## Runtime-compatibility regression evidence

```text
Runtime compatibility focused regression
Total:     65
Succeeded: 65
Failed:    0
Skipped:   0
Result:    PASS
```

The regression correction preserves the frozen runtime verifier semantics while advancing fixtures from migration 004 to migration 005:

- pending history is the exact `1,2,3,4` prefix;
- database-newer scenarios begin at migration ID 6;
- legacy-adoption scenarios physically contain only migrations 001..004 without a ledger;
- current schema remains post-005.

No production runtime-classifier or verifier semantic change was required.

## Previously failing full-suite regression set

After correcting stale migration-004 fixture assumptions:

```text
Focused affected-test rerun
Total:     62
Succeeded: 62
Failed:    0
Skipped:   0
Result:    PASS
```

This covered:

- repository schema descriptor tests;
- owned-object recognition tests;
- SQL schema metadata reader integration tests;
- migration execution integration tests;
- migration-003 hardening tests;
- migration concurrency conformance tests;
- runtime compatibility matrix tests;
- runtime compatibility state-conformance tests;
- persistence startup real-SQL success tests;
- runtime compatibility diagnostic tests.

The corrected distinction is:

```text
LegacyPost004 = historical 13-table descriptor
Current       = post-005 20-table descriptor
OwnedObjects  = current 20-table recognition authority
```

## Complete integration evidence

```text
FactoryConnect.Integration.Tests
Total:     699
Succeeded: 699
Failed:    0
Skipped:   0
Duration:  1683.3s
Result:    PASS
```

Build result:

```text
Build succeeded in 1723.6s
```

Repository whitespace gate:

```text
git diff --check
Result: CLEAN
```

## Architecture / scope containment review

The final implementation preserves the frozen boundaries:

- migration 005 physical DDL remains unchanged by the catalog-rendering correction;
- `LegacyPost004` remains unchanged as historical adoption authority;
- `SqlFragmentCanonicalizer` remains unchanged;
- `SqlSchemaComparator` remains unchanged;
- `SqlServerSchemaMetadataReader` semantics remain unchanged;
- FC-030.1B-R computed metadata remains verbatim from SQL Server;
- there is exactly one executable post-005 descriptor authority;
- no duplicate catalog transformation/rewrite layer remains;
- migration-005 catalog-rendered CHECK/filter/computed expectations live directly in `SqlRepositoryPost005SchemaDescriptor`;
- legacy post-004 adoption advances transactionally through migration 005;
- runtime verification remains read-only.

## Conclusion

All FC-030.2B implementation and regression gates are green. No blocking architectural or code-level finding remains in the implementation or correction set.

FC-030.2B is ready for final closure/freeze.