# FC-030.2A — Physical Authority Closure

## Status

**CLOSED / FROZEN.**

This closure record is the final gate for FC-030.2A physical authority and supersedes earlier provisional status headers such as `ACTIVE / PHYSICAL AUTHORITY FINAL REVIEW` or `REAL-SQL PROOF PENDING` in the companion review documents.

FC-030.2B is now authorized to implement migration `005_OperationalMetricReportingPersistence.sql` and the corresponding `CurrentSchemaDescriptor` / `OwnedObjectRecognitionSet` delta exactly as frozen here and in the referenced authorities.

---

# 1. Frozen authorities consumed

```text
FC-030.1 migration/schema/startup authority       CLOSED / FROZEN
FC-030.2A codec V1 contract                       CLOSED / FROZEN
B5 raw BIN2 characterization                      CLOSED / FAILED
StringOrderKeyV1                                  CLOSED / FAILED
StringOrderKeyV2 characterization                 PASS
SQL string-comparison ownership map               CLOSED / FROZEN
seven-table physical invariant matrix             CLOSED / FROZEN by this closure
real-SQL physical-authority evidence              PASS
migration-005 owned object names                  CLOSED / FROZEN
post-005 CurrentSchemaDescriptor delta             CLOSED / FROZEN
```

Normative files:

```text
docs/features/FC-030.2A-codec-v1-contract.md
docs/features/FC-030.2A-sql-production-reporting-persistence-contract.md
docs/features/FC-030.2A-seven-table-invariant-matrix.md
docs/features/FC-030.2A-b5-sql-ordinal-string-parity-evidence-2026-09-06.md
docs/features/FC-030.2A-string-order-key-v2-characterization.md
docs/features/FC-030.2A-string-comparison-ownership-map.md
docs/features/FC-030.2A-physical-authority-sql-evidence-2026-09-06.md
docs/features/FC-030.2A-migration-005-owned-object-names.md
docs/features/FC-030.2A-current-schema-descriptor-post005.md
```

---

# 2. Closed blocker state

```text
B1 nullable CHECK totality
  DESIGN RESOLVED
  REAL-SQL PROOF PASS
  CLOSED

B2 evidence ordinal gaps
  SQL nonnegative + unique
  writer exact 0..N-1 per kind
  reader contiguity revalidation
  CLOSED

B3 roster key/index type
  exact V2 identity
  PRIMARY KEY NONCLUSTERED
  ~1566-byte max key real-SQL creation PASS
  duplicate rejection PASS
  backing-index metadata PASS
  CLOSED

B4 processor mutual validation
  ProcessorKeyBinary varbinary(769)
  exact StringOrderKeyV2(ProcessorKey)
  sole SQL lookup/UQ/CAS-lock authority
  x vs x\u0000 distinct UQ slot PASS
  exact lookup PASS
  UPDLOCK,HOLDLOCK exact slot PASS
  769-byte maximum key PASS
  writer/reader binding revalidation frozen
  CLOSED

B5 raw BIN2 ordinal parity
  CLOSED — FAILED
  raw nvarchar comparison forbidden as provider-neutral authority
  replacement StringOrderKeyV2 incorporated
```

---

# 3. Frozen ordinal-string SQL authority

Provider-neutral semantic authority remains:

```text
StringComparer.Ordinal
```

Whenever SQL equality/order has semantic meaning before reconstruction:

```text
StringOrderKeyV2
  each validated UTF-16 code unit u -> 0x01 || UInt16BE(u)
  final terminator -> 0x00
  max 769 bytes
```

Exactly 13 companion V2 columns exist across projection/evidence/roster tables.

`OperationalMetricProjectionProcessor.ProcessorKeyBinary` is itself V2-encoded and therefore is not counted as a fourteenth companion column.

Raw `nvarchar` remains reconstruction/diagnostic storage only where mapped V2 authority exists.

---

# 4. Frozen physical boundary

Migration 005 creates exactly seven tables:

```text
dbo.OperationalMetricProjectionProcessor
dbo.OperationalMetricProjectionCheckpoint
dbo.OperationalMetricProjection
dbo.OperationalMetricProjectionManifest
dbo.OperationalMetricProjectionEvidence
dbo.MachineShiftOccurrenceRoster
dbo.MachineShiftOccurrenceRosterOccurrence
```

No aggregate-history table is added.

The exact columns, types, nullability, collation, identity metadata, computed MachineOrderKey definition, PK/UQ/FK/CHECK/index structures, backing-index types, and deterministic owned names are frozen by:

```text
FC-030.2A-migration-005-owned-object-names.md
FC-030.2A-current-schema-descriptor-post005.md
```

`LegacyPost004SchemaDescriptor` remains unchanged.

---

# 5. Real-SQL closure evidence

Observed on:

```text
SQL Server 17.0.1000.7 / RTM
Express Edition (64-bit)
```

PASS:

```text
processor V2 UQ / lookup / UPDLOCK,HOLDLOCK
769-byte processor key
~777-byte evidence operand UQ
~788-byte roster parent UQ
~1566-byte NONCLUSTERED roster occurrence PK
nullable CHECK totality fixtures
MachineOrderKey PERSISTED + filtered-index compatibility
Guid V1 known/boundary/10,000 random parity
SQL SHA2_256 vs .NET including 1 MiB varbinary(max)
processor UQ backing index metadata
roster occurrence NONCLUSTERED primary-key metadata
```

Final harness line:

```text
FC-030.2A PHYSICAL-AUTHORITY CANDIDATE: PASS
```

This evidence is promoted to frozen physical-authority closure by this document.

---

# 6. FC-030.2B authorization

FC-030.2B may now:

```text
create src/FactoryConnect.Persistence.SqlServer/Sql/005_OperationalMetricReportingPersistence.sql
add migration 005 to the canonical migration catalog
implement the exact seven-table schema
implement exact deterministic names/backing-index types
add the exact post-005 CurrentSchemaDescriptor delta
add the exact OwnedObjectRecognitionSet delta
add migration/schema conformance tests
```

FC-030.2B must NOT:

```text
change migrations 001..004
change LegacyPost004SchemaDescriptor
change FC-030.1 ledger/applock/C/D/E semantics
change frozen codec V1 semantics
change StringOrderKeyV2 semantics
add/remove V2 comparison ownership
change any frozen owned object name/type/mapping
activate runtime SQL capabilities
```

Capability activation remains owned by later FC-030.2C/2D/2E slices.

---

# 7. Architecture stop rule

If implementation of migration 005 or `CurrentSchemaDescriptor` requires any new semantic or physical design decision rather than expressing these frozen authorities, stop FC-030.2B and return to architecture review.

Implementation-local convenience is not authority to change the frozen contract.

---

# 8. Final state

```text
FC-030.2A codec architecture       CLOSED / FROZEN
FC-030.2A physical authority       CLOSED / FROZEN

B1                                CLOSED
B2                                CLOSED
B3                                CLOSED
B4                                CLOSED
B5 raw BIN2                       CLOSED / FAILED
StringOrderKeyV1                  CLOSED / FAILED
StringOrderKeyV2                  CLOSED / FROZEN AS SQL COMPARISON KEY

owned object names                CLOSED / FROZEN
post-005 CurrentSchemaDescriptor  CLOSED / FROZEN
real-SQL physical proof           PASS / CLOSED

Migration 005                     AUTHORIZED FOR FC-030.2B
FC-030.2B                         READY TO START
```
