# FC-030.2A — SQL String Comparison Ownership Map

## Status

**REVIEWED / PHYSICAL-CONTRACT INPUT.**

This document is the exact seven-table inventory of persisted string columns and whether SQL is permitted to use them for provider-neutral equality/order before reader reconstruction.

It consumes unchanged:

- `FC-030.2A-codec-v1-contract.md` — frozen `CanonicalStringV1`;
- `FC-030.2A-b5-sql-ordinal-string-parity-evidence-2026-09-06.md` — BIN2 negative characterization;
- `FC-030.2A-string-order-key-v2-characterization.md` — `StringOrderKeyV2` positive candidate characterization.

Migration `005_OperationalMetricReportingPersistence.sql` remains absent. This map does not freeze object names or physical authority by itself.

---

# 1. Comparison rule

Provider-neutral string identity/order authority is:

```text
StringComparer.Ordinal
```

Raw SQL `nvarchar` comparison is not semantic authority.

Whenever SQL equality/order has semantic effect before reader reconstruction, the row persists both:

```text
Value      nvarchar(...)
OrderKey   varbinary(769)
```

where `OrderKey = StringOrderKeyV2(Value)`.

`StringOrderKeyV2` is writer-constructed and reader-revalidated. SQL owns only outer nullability/length and the relational comparison over the binary key. Any text/key mismatch is persisted-data corruption.

Storage-only strings retain their `nvarchar` value without an order-key companion. They are validated/reconstructed by the reader and MUST NOT be used as provider-neutral SQL equality/order authority later without a new architecture review.

---

# 2. Exact seven-table ownership map

## 2.1 dbo.OperationalMetricProjectionProcessor

| String column | SQL semantic use | Comparison authority | V2 companion |
|---|---|---|---|
| `ProcessorKey` | diagnostics/reconstruction only | `ProcessorKeyBinary` owns lookup/UQ/lock identity; B4 reader proves text/binary/binding agreement | **No** |

Required rule:

```text
SQL processor lookup / uniqueness / first CAS lock target
  -> ProcessorKeyBinary

ProcessorKey text
  -> never independent SQL identity authority
```

## 2.2 dbo.OperationalMetricProjectionCheckpoint

No persisted string columns.

## 2.3 dbo.OperationalMetricProjection

| String column | SQL semantic use | Comparison authority | V2 companion |
|---|---|---|---|
| `PeriodSiteId` | reporting ORDER BY + startAfter tail | V2 | `PeriodSiteOrderKey` |
| `ShiftScheduleAssignmentId` | shift ORDER BY + startAfter tail | V2 | `ShiftScheduleAssignmentOrderKey` |
| `ShiftId` | shift ORDER BY + startAfter tail | V2 | `ShiftOrderKey` |
| `ProductionOrderId` | context equality filter + ORDER BY/startAfter when present | V2 | `ProductionOrderOrderKey` |
| `OperationId` | context equality filter + ORDER BY/startAfter when present | V2 | `OperationOrderKey` |
| `PartId` | context equality filter + ORDER BY/startAfter when present | V2 | `PartOrderKey` |
| `OperatorId` | context equality filter + ORDER BY/startAfter when present | V2 | `OperatorOrderKey` |
| `MetricKey` | metric equality filter + ORDER BY/startAfter | V2 | `MetricKeyOrderKey` |
| `DefinitionVersion` | logical metric-definition tail equality/order | V2 | `DefinitionVersionOrderKey` |
| `MetricValue` | nullable payload only; canonical decimal reader authority | no SQL ordinal identity/order | **No** |
| `Unit` | reconstruction/output only | no SQL ordinal identity/order | **No** |
| `ReasonOperandName` | reconstruction/output only | no SQL ordinal identity/order | **No** |

Exactly nine projection V2 columns are required.

For optional contexts, the order key has the same presence as the value:

```text
Present = 0 -> Value IS NULL AND OrderKey IS NULL
Present = 1 -> Value IS NOT NULL AND OrderKey IS NOT NULL
```

## 2.4 dbo.OperationalMetricProjectionManifest

No persisted string columns.

## 2.5 dbo.OperationalMetricProjectionEvidence

| String column | SQL semantic use | Comparison authority | V2 companion |
|---|---|---|---|
| `OperandName` | cross-kind uniqueness per projection | V2 | `OperandNameOrderKey` |
| `ComponentKey` | reconstruction only | reader | **No** |
| `ComponentValue` | canonical decimal payload | reader | **No** |
| `ComponentUnit` | reconstruction only | reader | **No** |
| `DependencyMetricKey` | structured header validated against nested FCDS | reader | **No** |
| `DependencyDefinitionVersion` | structured header validated against nested FCDS | reader | **No** |

Evidence list ordering is by `EvidenceKind, EvidenceOrdinal`, not by `OperandName`; the V2 key exists solely for provider-neutral cross-kind operand equality.

## 2.6 dbo.MachineShiftOccurrenceRoster

| String column | SQL semantic use | Comparison authority | V2 companion |
|---|---|---|---|
| `ProductionDaySiteId` | roster logical UQ + CAS lookup/lock | V2 | `ProductionDaySiteOrderKey` |
| `ProductionLineId` | reconstruction/output only | reader | **No** |

Roster logical identity becomes:

```text
MachineId
ProductionDaySiteOrderKey
ProductionBusinessDate
```

The text `ProductionDaySiteId` remains the reconstructed identity value and must match the V2 key on read.

## 2.7 dbo.MachineShiftOccurrenceRosterOccurrence

| String column | SQL semantic use | Comparison authority | V2 companion |
|---|---|---|---|
| `ShiftScheduleAssignmentId` | complete occurrence UQ/PK + deterministic order | V2 | `ShiftScheduleAssignmentOrderKey` |
| `ShiftId` | complete occurrence UQ/PK + deterministic order | V2 | `ShiftOrderKey` |

Complete SQL occurrence identity becomes:

```text
MachineShiftOccurrenceRosterRowId
ShiftScheduleAssignmentOrderKey
ShiftOrderKey
ShiftStartsAtUtc
ShiftEndsAtUtc
```

The text values remain reconstruction authority and are reader-revalidated against the keys.

---

# 3. Exact V2 column count

The seven-table physical contract requires exactly these 13 companion columns:

```text
OperationalMetricProjection
  PeriodSiteOrderKey
  ShiftScheduleAssignmentOrderKey
  ShiftOrderKey
  ProductionOrderOrderKey
  OperationOrderKey
  PartOrderKey
  OperatorOrderKey
  MetricKeyOrderKey
  DefinitionVersionOrderKey

OperationalMetricProjectionEvidence
  OperandNameOrderKey

MachineShiftOccurrenceRoster
  ProductionDaySiteOrderKey

MachineShiftOccurrenceRosterOccurrence
  ShiftScheduleAssignmentOrderKey
  ShiftOrderKey
```

No additional V2 column is introduced merely because a field is a string.

---

# 4. Required V2 outer SQL shape

Required/non-empty semantic string:

```sql
Value IS NOT NULL
AND DATALENGTH(Value) > 0
AND OrderKey IS NOT NULL
AND DATALENGTH(OrderKey) BETWEEN 1 AND 769
```

Optional context string:

```sql
(
    Present = 0
    AND Value IS NULL
    AND OrderKey IS NULL
)
OR
(
    Present = 1
    AND Value IS NOT NULL
    AND DATALENGTH(Value) > 0
    AND OrderKey IS NOT NULL
    AND DATALENGTH(OrderKey) BETWEEN 1 AND 769
)
```

SQL does not prove `OrderKey = StringOrderKeyV2(Value)`. That mutual semantic invariant is:

```text
Writer construction invariant + reader revalidation
```

The reader must re-encode the exact reconstructed .NET string and require byte-for-byte equality with the persisted key before the row is accepted.

---

# 5. Query authority after the map

Reporting tail SQL becomes:

```text
Shift
  ShiftStartsAtUtc
  MachineOrderKey
  PeriodSiteOrderKey
  ShiftScheduleAssignmentOrderKey
  ShiftOrderKey
  ShiftEndsAtUtc
  ProductionOrderPresent, ProductionOrderOrderKey when present
  OperationPresent, OperationOrderKey when present
  PartPresent, PartOrderKey when present
  OperatorPresent, OperatorOrderKey when present
  MetricKeyOrderKey
  DefinitionVersionOrderKey

ProductionDay
  ProductionBusinessDate
  MachineOrderKey
  PeriodSiteOrderKey
  ProductionOrderPresent, ProductionOrderOrderKey when present
  OperationPresent, OperationOrderKey when present
  PartPresent, PartOrderKey when present
  OperatorPresent, OperatorOrderKey when present
  MetricKeyOrderKey
  DefinitionVersionOrderKey
```

Only the primary period scalar reverses for descending queries. All V2 tail keys remain ascending, exactly matching the provider-neutral comparer.

String equality filters bind the V2 key generated from the user/provider-neutral string; they do not compare the raw `nvarchar` value.

---

# 6. Recalculated PK/UQ/index widths

Using maximum `StringOrderKeyV2 = 769` bytes:

```text
Projection logical hash UQ
  ProjectionProcessorRowId bigint      8
  EvaluationKeyHash binary(32)        32
  total                              ~40
  -> unaffected

Projection child-authority UQ
  ProjectionProcessorRowId bigint      8
  ProjectionRowId bigint                8
  total                              ~16
  -> unaffected

Evidence cross-kind operand UQ
  ProjectionRowId bigint                8
  OperandNameOrderKey                 769
  total                             ~777
  -> below 900-byte clustered envelope and 1700-byte nonclustered envelope

Evidence ordinal UQ
  ProjectionRowId bigint                8
  EvidenceKind tinyint                  1
  EvidenceOrdinal int                   4
  total                              ~13
  -> unaffected

Roster parent logical UQ
  MachineId uniqueidentifier           16
  ProductionDaySiteOrderKey           769
  ProductionBusinessDate date           3
  total                             ~788
  -> below 900-byte clustered envelope and 1700-byte nonclustered envelope

Roster occurrence complete identity
  RosterRowId bigint                     8
  ShiftScheduleAssignmentOrderKey      769
  ShiftOrderKey                        769
  ShiftStartsAtUtc datetimeoffset(7)    10
  ShiftEndsAtUtc datetimeoffset(7)      10
  total                              ~1566
  -> above 900 clustered envelope
  -> below 1700 modern nonclustered envelope
  -> MUST be explicit NONCLUSTERED
```

The two reporting access indexes remain narrow and unchanged because their key columns are numeric/date/Guid-order/surrogate fields only:

```text
ShiftWindow
  ProjectionProcessorRowId
  ShiftStartsAtUtc
  MachineOrderKey
  ProjectionRowId

ProductionDayWindow
  ProjectionProcessorRowId
  ProductionBusinessDate
  MachineOrderKey
  ProjectionRowId
```

V2 tail fields may be read/sorted from the narrowed result set; they are not added to the access-index key merely to avoid a SQL sort.

---

# 7. Consequences for B1-B4

## B1 — total nullable CHECK predicates

Resolution must include V2 nullability in every affected period/context shape. No `CHECK` may rely on SQL `UNKNOWN`. `DATALENGTH`, not `LEN`, is the non-empty mechanical check.

## B2 — evidence ordinal contiguity

Unchanged by V2:

```text
writer emits 0..N-1 independently per EvidenceKind
reader orders by EvidenceKind, EvidenceOrdinal
reader requires exact contiguous sequence
```

## B3 — roster occurrence key

Resolution candidate is now exact:

```text
PRIMARY KEY NONCLUSTERED
(
  MachineShiftOccurrenceRosterRowId,
  ShiftScheduleAssignmentOrderKey,
  ShiftOrderKey,
  ShiftStartsAtUtc,
  ShiftEndsAtUtc
)
```

Real SQL maximum-width creation/duplicate/metadata proof remains required before physical freeze.

## B4 — projection processor mutual validation

`ProcessorKeyBinary` is the only SQL identity/lookup/lock authority. No `StringOrderKeyV2` is added for `ProcessorKey`.

Writer and reader must prove:

```text
ProcessorKeyBinary
ProcessorKey
MetricAggregationProcessorRowId
MetricInputStreamRowId
```

all reconstruct the same provider-neutral processor identity/binding. Mismatch is deterministic persisted-data corruption.

---

# 8. Closure state after ownership mapping

```text
B1 nullable CHECK totality       DESIGN RESOLVED — exact main-contract predicates still to freeze
B2 evidence ordinal gaps         DESIGN RESOLVED — writer + reader ownership fixed
B3 roster key/index type         DESIGN RESOLVED — V2 NONCLUSTERED candidate; real SQL proof pending
B4 processor mutual validation  DESIGN RESOLVED — binary identity + reader mutual validation
B5 BIN2 ordinal parity           CLOSED — FAILED
StringOrderKeyV1                 CLOSED — FAILED
StringOrderKeyV2                 CANDIDATE PASS
string comparison ownership      CLOSED / MAPPED

owned object-name freeze         BLOCKED UNTIL REAL-SQL PHYSICAL PROOF
real-SQL physical proof          PENDING
FC-030.2A physical authority     HOLD
Migration 005                    MUST REMAIN ABSENT
FC-030.2B                        NOT STARTED
```
