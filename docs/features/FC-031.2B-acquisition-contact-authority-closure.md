# FC-031.2B — Acquisition / Contact Authority Closure

Status: **CLOSED / FROZEN**

Parent feature: **FC-031 — Authoritative Current Machine State**

## Closure boundary

FC-031.2B establishes the portable acquisition/contact authority required by authoritative current machine state. It closes the acquisition-side authority seam without authorizing mapping-coverage, state/activity continuity, current-state provider, semantic-reader, API, or dashboard work.

The slice is provider-neutral at its semantic boundary. In-memory and SQL Server behavior are conformance implementations of the same authority rather than independent specifications.

## Frozen outcomes

The completed slice establishes and proves the following authority:

- acquisition/contact facts are durable, stream-scoped authority suitable for later current-state evaluation;
- authority is correlated through the existing `ObservationStreamId` ownership boundary and remains independent across streams;
- acquisition/contact revision behavior is deterministic and provider-neutral;
- first authoritative persistence establishes the first revision consistently;
- subsequent authoritative writes advance revision according to the frozen contract;
- checkpoint/observation relational integrity is preserved rather than weakened by cascade behavior;
- SQL Server uses explicit `NO ACTION` relational semantics at the authority boundary;
- exhaustion and checkpoint fixtures remain revision-coherent;
- migration 007 owns the SQL schema delta required by this authority, including legacy backfill behavior;
- migration catalog/checksum/current-schema authority is updated consistently with migration 007;
- the SQL migration path is proven for both legacy upgrade/backfill and current-schema compatibility;
- in-memory and SQL Server providers satisfy the shared acquisition-authority conformance suite;
- SQL-specific relational tests add provider evidence without redefining portable semantics;
- culture-sensitive conversion is excluded from SQL conformance evidence by using invariant conversion.

## Evidence trail

The branch contains dedicated conformance and repair commits for:

- migration 007 binding and checksum/catalog authority;
- legacy migration-007 backfill proof;
- cross-provider acquisition conformance;
- revision-coherent SQL exhaustion fixtures;
- SQL `NO ACTION` and first-revision semantics;
- isolated checkpoint foreign-key proof;
- culture-invariant SQL count conversion.

Final reviewed branch head before this closure record:

```text
e03e6c47a13e02394b1510ca2619e5fe361bdbd1
FC-031.2B — make SQL count conversion culture-invariant
```

The final local verification reported for this slice is:

```text
FactoryConnect.Integration.Tests
393 passed
0 failed
0 skipped

git diff --check
clean
```

## Frozen exclusions

FC-031.2B does **not** establish or authorize:

- canonical signal mapping coverage authority;
- rules for whether an authoritative evidence owner has sufficient mapped inputs to evaluate current state;
- state/activity continuity or freshness semantics;
- `Offline` synthesis;
- in-memory authoritative current-state cut materialization;
- Core semantic current-state reading;
- SQL current-state authority beyond the acquisition/contact persistence added here;
- HTTP/API contracts;
- dashboard behavior.

Those remain later FC-031 slices.

## Closure decision

FC-031.2B is complete. Its portable acquisition/contact semantics, provider conformance, migration-007 SQL authority, and relational evidence are now frozen.

The next authorized implementation slice is:

```text
FC-031.2C — Mapping Coverage Authority
```

FC-031.2C may consume the frozen FC-031.2A contract foundation and FC-031.2B acquisition/contact authority, but it must not reopen either slice unless current repository evidence demonstrates a concrete contradiction.
