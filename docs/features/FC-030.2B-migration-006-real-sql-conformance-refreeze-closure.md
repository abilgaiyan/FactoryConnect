# FC-030.2B — Migration 006 Real-SQL Conformance / Refreeze Closure

## Status

**CLOSED / FROZEN — POST-006 PHYSICAL AUTHORITY PROVEN.**

This document records the final focused real-SQL conformance evidence for the narrow FC-030.2A / FC-030.2B reopen caused by the `OperationalMetricProjectionManifest` checkpoint-parent foreign-key contradiction.

It closes only the physical correction boundary. It does **not** close FC-030.2C.3 or FC-030.2C.4; their joint publication/concurrency closure remains separately required.

---

## 1. Frozen correction

Historical Migration 005 remains immutable.

Migration 006 is the only authorized physical correction:

```text
MigrationId:   6
Name:          CorrectOperationalMetricProjectionManifestParent
Resource:      006_CorrectOperationalMetricProjectionManifestParent.sql
Policy:        EngineOwned
Checksum:      DDFD8C6CC1FADE9D7486EB5D3D915881A2538E2724A6B57EDA7E6D7C16EB6EEE
```

The migration performs only:

```text
DROP
  FK_OperationalMetricProjectionManifest_Checkpoint

ADD
  FK_OperationalMetricProjectionManifest_Processor
```

with the new mapping:

```text
OperationalMetricProjectionManifest
  .OperationalMetricProjectionProcessorRowId
→
OperationalMetricProjectionProcessor
  .OperationalMetricProjectionProcessorRowId
```

The existing composite manifest-to-projection FK remains unchanged.

Migration 005 historical SQL, canonical bytes, checksum, descriptor identity, and ledger identity remain unchanged.

---

## 2. Post-006 repository authority

Current repository schema authority is now:

```text
LegacyPost004
    ↓
Post005
    ↓
Post006
    ↓
Current
```

The post-006 descriptor delta changes only the manifest parent FK:

```text
remove:
  FK_OperationalMetricProjectionManifest_Checkpoint

add:
  FK_OperationalMetricProjectionManifest_Processor
```

No unrelated table, column, PK, UQ, index, CHECK, collation, computed/default definition, or foreign key is changed by the post-006 authority transition.

The migration catalog authority is now canonical `001..006`.

---

## 3. Real-SQL Migration 006 proof

The focused Migration 006 conformance tests prove:

```text
exact post-005
→ Migration 006 applies successfully
→ exact post-006 current schema
```

They also prove:

```text
old manifest→checkpoint FK absent
new manifest→processor FK present
new FK enabled
new FK trusted
new FK exact column mapping
composite manifest→projection FK unchanged
```

Existing processor/checkpoint/projection/manifest/evidence data is preserved across Migration 006.

Forced failure inside the Migration 006 transaction proves exact rollback to the post-005 physical and ledger state, followed by successful retry.

---

## 4. Two-caller serialized execution proof

The dedicated real-SQL concurrency proof starts from exact post-005 and runs two migration callers against the same database.

Observed sequence:

```text
caller 1
  → acquires the exclusive migration application lock
  → reaches Migration 006
  → is deliberately blocked at manifest DDL

caller 2
  → starts while caller 1 owns migration authority
  → is observed waiting behind caller 1's application lock

DDL blocker released

caller 1
  → applies Migration 006
  → records canonical migration 006
  → commits

caller 2
  → acquires the migration lock afterward
  → observes canonical 001..006
  → performs no duplicate Migration 006 application
  → completes successfully
```

Final assertions prove:

```text
both callers independently observe migration IDs 1..6
exactly one Migration 006 ledger row exists
Migration 006 name/checksum are canonical
Migration 006 AppliedAtUtc comes from caller 1's clock
new FK is trusted and enabled
live schema exactly matches Current
```

This closes the required serialized/concurrent-execution proof: Migration 006 is recorded exactly once under competing callers.

---

## 5. Verifier-level pre/post compatibility proof

The dedicated verifier proof starts from an exact post-005 physical schema with exact ledger prefix `001..005`.

Before Migration 006:

```text
SqlServerRuntimeSchemaCompatibilityVerifier
→ MigrationPending
→ IsCompatible = false
```

The verifier remains read-only and does not apply the missing migration.

After the migration engine applies Migration 006:

```text
SqlServerRuntimeSchemaCompatibilityVerifier
→ Compatible
→ IsCompatible = true
```

The ledger is exactly `001..006`, and the live physical schema exactly matches `SqlRepositorySchemaDescriptors.Current`.

This proves the required runtime boundary:

```text
exact post-005 = migration pending
exact post-006 = compatible/current
```

---

## 6. Focused proof results

### Dedicated new proofs

Command:

```text
dotnet test tests/FactoryConnect.Integration.Tests/FactoryConnect.Integration.Tests.csproj -c Release --filter "FullyQualifiedName~SqlServerMigration006ConcurrencyIntegrationTests|FullyQualifiedName~SqlServerMigration006VerifierCompatibilityIntegrationTests"
```

Observed result:

```text
FactoryConnect.Integration.Tests test net10.0 succeeded

Total:      2
Succeeded:  2
Failed:     0
Skipped:    0
Test time:  76.5s
Build:      succeeded
Build time: 129.9s
```

### Complete focused/refreeze matrix

Command:

```text
dotnet test tests/FactoryConnect.Integration.Tests/FactoryConnect.Integration.Tests.csproj -c Release --filter "FullyQualifiedName~SqlMigration006AuthorityTests|FullyQualifiedName~SqlServerMigration006ConformanceIntegrationTests|FullyQualifiedName~SqlServerMigration006ConcurrencyIntegrationTests|FullyQualifiedName~SqlServerMigration006VerifierCompatibilityIntegrationTests|FullyQualifiedName~SqlServerMigrationConcurrencyConformanceIntegrationTests|FullyQualifiedName~SqlServerRuntimeSchemaCompatibilityVerifierIntegrationTests|FullyQualifiedName~SqlServerOperationalMetricProjectionPublicationIntegrationTests"
```

Observed result:

```text
FactoryConnect.Integration.Tests test net10.0 succeeded

Total:      25
Succeeded:  25
Failed:     0
Skipped:    0
Test time:  246.3s
Build:      succeeded
Build time: 261.3s
```

The focused matrix includes:

```text
Migration 006 immutable identity/checksum authority
post-006 descriptor delta authority
migration catalog/history authority through 006
exact post-005 → post-006 migration application
forced failure rollback + successful retry
trusted/enabled FK metadata proof
data-preservation proof
two-caller migration serialization
existing migration concurrency regression coverage
verifier-level post-005 pending → post-006 compatible proof
existing runtime verifier regression coverage
unchanged FC-030.2C.4 publication transitions
```

---

## 7. C.4 blocker resolution

The original seven C.4 publication failures were caused by the Migration 005 manifest-to-checkpoint FK, not by the frozen C.4 mutation sequence.

After the Migration 006 physical correction, the unchanged publication integration suite passes:

```text
SqlServerOperationalMetricProjectionPublicationIntegrationTests

Total:      7
Succeeded:  7
Failed:     0
Skipped:    0
```

This proves the blocker is resolved without changing:

```text
C.2 checkpoint-last choreography
Serializable writer isolation
processor → checkpoint → projection → manifest → evidence lock order
C.4 dependency-safe DML order
ReconcileProposed validation-only semantics
```

No checkpoint-early workaround was introduced.

---

## 8. Refrozen authority

The narrow reopen is now closed.

```text
FC-030.2A
  manifest parent-FK correction
  CLOSED / FROZEN

FC-030.2B
  Migration 006 identity
  Migration 006 canonical SQL/checksum
  post-006 descriptor
  migration catalog/history through 006
  real-SQL migration application
  rollback/retry
  concurrent exactly-once execution
  verifier pre/post compatibility
  CLOSED / FROZEN

Migration 005
  HISTORICAL / IMMUTABLE

Migration 006
  CURRENT PHYSICAL CORRECTION AUTHORITY
  CLOSED / FROZEN
```

The former blocker state is superseded only with respect to implementation authorization and physical-resolution status. The original seven-failure blocker evidence remains historical evidence and must not be deleted or rewritten.

---

## 9. Explicit non-changes

This closure does not authorize any change to:

```text
Migration 005 SQL/checksum/history
Migration 006 SQL/checksum/identity
C.1 publication semantics
C.2 transaction/checkpoint choreography
C.3 projection acquisition semantics
Serializable / UPDLOCK / HOLDLOCK authority
publication lock ordering
C.4 mutation ordering
ReconcileProposed zero-DML behavior
reader old-complete/new-complete guarantee
```

Any change to those boundaries requires a separate architecture reopen.

---

## 10. Roadmap state after this closure

```text
FC-030.2A  Physical schema contract                         CLOSED / FROZEN
FC-030.2B  Migration 005 + 006 + CurrentSchemaDescriptor    CLOSED / FROZEN

FC-030.2C.1  Persistence contract                           CLOSED / FROZEN
FC-030.2C.2  Transaction/checkpoint foundation              CLOSED / FROZEN
FC-030.2C.3  Projection acquisition                         IMPLEMENTATION COMPLETE
                                                            CLOSURE DEFERRED
FC-030.2C.4  Atomic publication                             IMPLEMENTED
                                                            physical blocker RESOLVED
                                                            JOINT C.3/C.4 CLOSURE PENDING
```

Formal FC-030.2C.3 and FC-030.2C.4 closure remains intentionally coupled and must occur only after the complete publication/concurrency/failure-injection matrix is green.