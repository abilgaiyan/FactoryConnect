# FC-030.2A — B5 SQL Ordinal-String Parity Evidence — 2026-09-06

## Status

**B5 FAILED / CLOSED AS A NEGATIVE CHARACTERIZATION. PHYSICAL AUTHORITY REMAINS HOLD.**

This evidence was produced by the disposable `FactoryConnect.B5SqlOrdinalParityCheck` harness against:

```text
SQL Server ProductVersion  17.0.1000.7
ProductLevel               RTM
Edition                    Express Edition (64-bit)
Candidate collation        Latin1_General_100_BIN2
.NET target                net10.0
```

The frozen semantic authority remains `.NET StringComparer.Ordinal` over the already-frozen `CanonicalStringV1` domain.

Migration `005_OperationalMetricReportingPersistence.sql` MUST remain absent.

---

# 1. Pairwise equality/order failures

The complete rerun proves that `nvarchar(... COLLATE Latin1_General_100_BIN2)` is not equivalent to `StringComparer.Ordinal`.

Observed pairwise failures:

```text
trailing space
  left   "x"
  right  "x "
  .NET   Equals = false, Compare sign = -1
  SQL    Equals = true,  Compare sign = 0
  result equality FAIL / ordering FAIL

two trailing spaces
  left   "x "
  right  "x  "
  .NET   Equals = false, Compare sign = -1
  SQL    Equals = true,  Compare sign = 0
  result equality FAIL / ordering FAIL

trailing U+0000
  left   "x"
  right  "x\u0000"
  .NET   Equals = false, Compare sign = -1
  SQL    Equals = false, Compare sign = +1
  result equality PASS / ordering FAIL

double trailing U+0000
  left   "x\u0000"
  right  "x\u0000\u0000"
  .NET   Equals = false, Compare sign = -1
  SQL    Equals = false, Compare sign = +1
  result equality PASS / ordering FAIL
```

These failures alone reject `Latin1_General_100_BIN2` as the migration-005 ordinal string equality/order authority.

Other exercised pairs passed, including case distinction, composed/decomposed Unicode, BOM-leading strings, BMP/supplementary boundaries, adjacent supplementary scalars, and maximum-length vectors. Partial agreement is irrelevant because B5 requires exact parity.

---

# 2. Whole-corpus ORDER BY failure

The complete rerun also proved global ordering divergence.

```text
.NET order:
1, 2, 3, 4, 23, 12, 5, 6, 7, 11, 8, 9, 10, 13, 24, 14, 17, 18, 19, 25, 20, 21, 15, 22, 16

SQL order:
1, 2, 3, 4, 23, 12, 7, 6, 11, 5, 8, 9, 10, 13, 24, 14, 17, 18, 19, 25, 20, 21, 15, 22, 16
```

The divergence is concentrated in the `x`, `x\0`, and `x\0\0` region, proving SQL `ORDER BY ... COLLATE Latin1_General_100_BIN2` is not provider-neutral ordinal order.

---

# 3. Strict seek/pagination failure

Strict `startAfter` characterization failed at multiple cursor boundaries. The failure is semantic, not cosmetic: SQL returned rows in an order/membership sequence different from the provider-neutral ordinal tuple.

Permanent representative failures include cursors at:

```text
empty string
single space
"A"
"a"
maximum-length ASCII string
decomposed e-acute
"x"
"x\0"
"x\0\0"
"x\0y"
```

For example:

```text
cursor "x"
.NET expected next IDs:
6,7,11,8,9,10,13,24,14,17,18,19,25,20,21,15,22,16

SQL actual next IDs:
8,9,10,13,24,14,17,18,19,25,20,21,15,22,16
```

Therefore BIN2 cannot be used for FC-028-compatible continuation semantics.

---

# 4. UNIQUE enforcement failure

Real SQL UNIQUE characterization proved:

```text
"x"
"x "
```

are collapsed by the candidate SQL collation even though `StringComparer.Ordinal` treats them as distinct identities.

Observed result:

```text
UNIQUE treats "x" and "x " as equal but StringComparer.Ordinal does not
```

Therefore BIN2 cannot own cross-kind `OperandName` uniqueness, roster identity uniqueness, roster-occurrence identity uniqueness, or any other provider-neutral string-bearing UQ/PK semantics.

---

# 5. Architectural consequence

The current BIN2 relational representation cannot be frozen for any operation where SQL string comparison occurs before reconstruction, including:

```text
reporting ORDER BY
startAfter seek predicates
query equality filters
cross-kind OperandName uniqueness
projection-processor textual comparisons
machine-shift roster identity uniqueness
roster occurrence identity uniqueness
roster deterministic ordering
string-bearing PK/UQ/index semantics
```

Reader validation cannot repair membership/order/uniqueness that SQL has already changed.

Therefore:

```text
B5 SQL ordinal-string parity        FAIL / NEGATIVE CHARACTERIZATION CLOSED
Latin1_General_100_BIN2             NOT ACCEPTED as ordinal authority
FC-030.2A physical authority        HOLD
Migration 005                       MUST REMAIN ABSENT
FC-030.2B                           NOT STARTED
```

The frozen `CanonicalStringV1` domain is unchanged. No trimming, Unicode normalization, U+0000 stripping, BOM stripping, case folding, or domain narrowing is permitted as a fallback.

---

# 6. Replacement-direction constraint

Any replacement SQL comparison representation must preserve exact `.NET StringComparer.Ordinal` semantics.

Candidate direction:

```text
StringOrderKeyV1
  source: exact validated .NET UTF-16 code-unit sequence
  encoding: each UInt16 code unit as two big-endian bytes
  no terminator
  no normalization
  no trimming
  no escaping
  max payload: 256 UTF-16 code units -> 512 bytes
```

This is not yet frozen authority. It requires independent real-SQL proof for:

```text
exact equality parity
exact ordering parity
prefix behavior
trailing U+0020
embedded/trailing U+0000
BMP/supplementary boundaries
maximum-length values
ORDER BY
startAfter seek
filters
PK/UQ uniqueness
all affected key-width/index limits
writer construction + reader revalidation if stored rather than computed
```

If the replacement representation changes physical columns/indexes, the seven-table matrix, key-width review, object-name freeze, and CurrentSchemaDescriptor candidate must be updated before FC-030.2A can close.
